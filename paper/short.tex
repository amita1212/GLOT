\documentclass{article}
\usepackage{iclr2026_conference,times}

% Short form. The body is prose only -- roughly four pages -- and carries the
% question, the method, the choices that changed the answer, and the findings.
% Every table, the figure, and all supporting measurement live in the appendix.
%
% PRESENTATION RULE FOR THIS DRAFT: the result tables mirror the layout of
% Table 1 of the original GLOT paper -- backbone blocks, method rows, task
% columns, scores x100, best per column in bold, metric row under the header.
% The rows are our arms rather than competing poolers, because our question is
% which COMPONENT of GLOT does what, not whether GLOT beats mean-pooling.

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xcolor}

% \iclrfinalcopy turns off the anonymous author block and the grey
% line-number ruler in the margin. It also switches the running head to
% "Published as a conference paper at ICLR 2026", which is not true of this
% draft, so we blank the head with \lhead{} immediately after \maketitle.
\iclrfinalcopy

\title{Does Hyperbolic Geometry Help Graph-Based\\
Sentence Pooling? A Controlled Stage-wise Study}

\author{Amit Alfasi \And Avraham Ron}

\begin{document}

\maketitle
\lhead{}

\begin{abstract}
Hyperbolic space represents tree-like structure with less distortion than flat
space, and transformer token states are often described as tree-like. That
pairing has motivated hyperbolic versions of many models. We ask whether it
actually helps, on one architecture, under controlled conditions.

Our subject is \textsc{Glot} \citep{mantri2026glot}, a sentence pooler that
builds a graph over the tokens of a frozen language model, refines it with a
GNN, and aggregates it into a single vector. Geometry enters in exactly three
places --- how the graph is built (\textbf{Stage A}), how states are pooled
(\textbf{Stage B}), how messages combine (\textbf{Stage C}) --- so we replace
each independently and give all eight combinations the same search budget over
fifteen shared seeds.

The effects are conditional on stage, task, backbone and training regime: the
same substitution can help or hurt, and no stage has a single direction. The
largest appear under contrastive training with zero-shot evaluation, where arms
containing the hyperbolic readout are worth $+17.1$ Spearman on STS12 and
$-15.5$ max-AP on TwitterSemEval2015. Under supervised fine-tuning that same readout is reliably
harmful --- negative on two encoders and a decoder, by up to $10.3$ MCC, with a
seed-variance signature that suggests why --- but the harm does not survive
scale: on SST-2, eight times larger than any other task we ran, it is no longer
distinguishable from the baseline. Stage C's apparent CoLA gain is not
curvature. A matched $2\times2$ at $65$ seeds, run after closing two
control-parity defects we found in our own code, puts the geometry term at
$+0.50$ (n.s.) against $+1.14$ for the configuration it was confounded with.
Stage A separates from the baseline on CoLA at fifty seeds, and is
significantly negative on six of seven RoBERTa retrieval tasks.

Two methodological findings travel beyond this architecture. Low
$\delta$-hyperbolicity, the usual justification for going hyperbolic, is an
artifact of a few very large token norms: on an outlier-free metric the
backbones it is used to tell apart are indistinguishable ($0.2127$ against
$0.2114$), while the Euclidean version differs between them by two orders of
magnitude. And a seed does not pin a run --- re-running one configuration at one
seed moves CoLA by up to $3.6$ MCC, more than the effects being measured.
\end{abstract}

%-------------------------------------------------------------------------
\section{Introduction}

Turning a language model's token states into one vector is a first step for
most sentence tasks. Simple poolers --- mean, max, \texttt{[CLS]} --- treat
tokens as an unordered bag. A newer idea is to treat pooling as \emph{relations
first, aggregation second}: build a graph over tokens, refine it with a GNN,
then aggregate. \textsc{Glot} \citep{mantri2026glot} is a strong example,
matching competitive GLUE and MTEB numbers with $20\times$ fewer trained
parameters and a frozen backbone. Our runs support the claim it rests on: on
the authors' own noise test, \textsc{Glot} beats every bag-of-tokens pooler by
$14$--$35$ points at every distractor ratio, in every seed
(Appendix~\ref{app:stress}).

This paper asks what else that design can carry. Hyperbolic space is a natural
candidate for all three of its parts, so we build a hyperbolic version of each
and measure them separately. That separation is the point: two of the three
move in opposite directions, so a single ``hyperbolic'' switch would have
reported their average and found nothing.

\paragraph{Contributions.}
\begin{enumerate}
\itemsep1pt
\item \textbf{A stage-wise hyperbolic study} (\S\ref{sec:results}). Under an
      equal search budget with $15$ shared seeds, effects differ by stage,
      task, backbone and training regime. We treat the resolved win/loss tally
      as a descriptive appendix summary, not evidence for a universal direction
      (Appendix~\ref{app:results}).
\item \textbf{A seed is not a run} (\S\ref{sec:choices}). Without deterministic
      kernels, re-running one configuration at one seed moves CoLA further than
      any effect this paper reports. This is not a caveat we inherited from
      elsewhere; it changed one of our own numbers by a factor of six.
\item \textbf{The usual motivation does not survive measurement}
      (\S\ref{sec:premise}). Low $\delta$-hyperbolicity is an artifact of a
      handful of very large token norms.
\item \textbf{The choices that changed our answers} (\S\ref{sec:choices}). A
      cache-warming order worth $5$ MCC, a search that reverses arm rankings, a
      threshold that silently produces a complete graph on RoBERTa, and four
      defects in the released code.
\end{enumerate}

%-------------------------------------------------------------------------
\section{What we changed}
\label{sec:method}

Let $x_1,\dots,x_L$ be the token states of a frozen backbone. \textsc{Glot}
joins two tokens when their cosine passes a threshold,
\begin{equation}
A_{ij} = \mathbf{1}\!\left[\, \cos(x_i, x_j) > \tau \,\right],
\label{eq:adj}
\end{equation}
runs $K$ graph-attention layers over the result, and aggregates with a learned
readout. Only the pooler trains. Geometry enters in exactly three places, so we
replace each in turn. Appendix~\ref{app:math} gives the Poincar\'e ball, the
maps $\exp^c_0$ and $\log^c_0$, and the two replaced operators in full;
Figure~\ref{fig:overview} shows where each stage sits.

\paragraph{Stage A --- the graph.} Lift the tokens into the ball and join them
when their geodesic distance falls below the $q$-quantile of all pairwise
distances. Using a quantile rather than a fixed radius fixes the edge count at
$q$, so Stage A and the cosine baseline are compared at equal sparsity.

\paragraph{Stage B --- the readout.} Replace the weighted sum with the Einstein
gyro-midpoint in Klein coordinates, then map back into the tangent space for the
task head. Unlike a plain mean this depends on curvature: as $c \rightarrow 0$
it degenerates to the arithmetic mean.

\paragraph{Stage C --- the message passing.} Replace each GNN layer with a
M\"obius one that lifts to the tangent space to combine neighbours, using
$W \otimes_c h = \exp^c_0(W \log^c_0 h)$ and either degree-based (HGCN) or
attention (HGAT) weights.

\paragraph{Curvature is searched in a scale-free way.} Curvature and feature
scale are not separately identifiable here: geodesic distance depends only on
$\sqrt{c}\,\|x\|$, so doubling the features and quartering $c$ leaves every
distance unchanged. We therefore rescale features to unit mean norm and search
the \emph{effective radius} $r_{\text{eff}} = \sqrt{c}\,\mathbb{E}\|x\|$ over
$\{0.5, 1, 1.5, 2, 3\}$ rather than $c$ itself. This is not cosmetic: at raw
BERT token norms ($\approx 14.7$) the map $\exp^c_0$ saturates in
\texttt{float32} and any fixed distance threshold below about $9$ returns an
\emph{empty} graph (Appendix~\ref{app:defects}).

We run all eight combinations of A, B and C, plus a \texttt{no graph} control
that deletes the token graph but keeps the trained projection and readout, and
on RoBERTa two threshold-calibration arms (Appendix~\ref{app:calib}).

%-------------------------------------------------------------------------
\section{How we ran it}
\label{sec:choices}

Every arm is frozen-backbone, $2$ epochs, Adam, batch $32$; only the pooler
trains. Tuning draws $40$ configurations at one held-out seed ($10$ on the
decoder, where a run costs four times as much); confirmation re-runs the winner
on the shared seed set $\{1,\dots,15\}$. We report the exact two-sided sign test
beside the paired $t$-test, call a result significant only when both agree, and
apply a Bonferroni correction across the pre-specified contrasts sharing a
baseline within a campaign; for MTEB, this family is every complete arm-by-task
contrast for a backbone (Appendix~\ref{app:protocol}).

Six choices moved our numbers further than the effects we were trying to
measure; Appendix~\ref{app:choices} documents all six. One of them changes how
the rest of this paper should be read. \textbf{A seed does not pin a run.} The
released code seeds Python, NumPy and Torch but never requests deterministic
kernels, and the scatter reductions under message passing are non-deterministic
on CUDA. Re-running one configuration at one seed moves CoLA by up to $3.61$
MCC, with paired $\mathrm{sd}=1.47$ --- larger than any effect we report
(Appendix~\ref{app:determinism}). Two things follow. Differencing a cell from
one campaign against a cell from another is not sound, so we mark every such
comparison $\S$ and decline to test it. And pairing recovers less than we
claimed: it halves the variance on STS-B and \emph{increases} it on CoLA, where
the run-to-run term dominates the seed.

%-------------------------------------------------------------------------
\section{What we found, stage by stage}
\label{sec:results}

Table~\ref{tab:absolute} gives every absolute score, laid out like Table 1 of
\citet{mantri2026glot}; Table~\ref{tab:verdict} gives the paired differences
this section discusses. Both are in Appendix~\ref{app:results}.

%-------------------------------------------------------------------------
\subsection{Stage A --- the hyperbolic graph: positive on one task, negative on retrieval}
\label{sec:stageA}

Stage A is nominally ahead of the baseline in eight of the eleven fine-tuning
settings we ran it in (Table~\ref{tab:verdict}), and ahead at all four
distractor ratios of the noise diagnostic (Appendix~\ref{app:stress}), but at
$n=15$ it reached significance in none of them: the strongest was CoLA, at
$t=2.37$ with a sign-test $p$ of $0.118$, which fails our both-tests rule.

\paragraph{So we powered it and it separated.} CoLA was the cheapest setting in
which to settle this, and we committed in advance to $n=50$ and to reporting
whatever came back. Over $50$ paired seeds Stage A gives $\bar{d}=+0.834$ MCC
($46.11 \pm 1.54$ against $45.27 \pm 1.68$), $t(49)=3.57$, $p=0.0008$, sign test
$p=0.0153$, $34/16$ seeds positive --- significant on both tests. Because this
is a two-arm re-test of a single pre-specified hypothesis rather than a nine-arm
sweep, the correction is over one comparison, and it survives. The $35$ newly
run seeds agree with the $15$ carried over from the wide campaign ($+0.892$
against $+0.700$), so the effect is not an artifact of the seeds that suggested
it.

\textbf{Verdict: positive on CoLA, null on the other fine-tuning settings, and
negative on retrieval.} This is the one place where more seeds converted a trend
into a result, and it is worth being precise about how little that buys: CoLA is
one fine-tuning setting of eleven, the effect is $0.83$ MCC, and Stage A remains
flat on STS-B, both MRPC columns, BERT and RoBERTa RTE and both decoder columns.
A sign test over the other ten fine-tuning settings gives $p=0.34$. And under
contrastive training it is not merely null: on RoBERTa retrieval Stage A is
significantly negative on six of seven tasks, four of them surviving Bonferroni
at $0/15$ seeds (Appendix~\ref{app:mteb3}). That is the same direction as the
density repair of \S\ref{sec:porting}, which also replaces RoBERTa's
near-complete cosine graph with a sparser one and also costs accuracy. We claim
Stage A helps on CoLA; we do not claim the Poincar\'e graph helps in general.

%-------------------------------------------------------------------------
\subsection{Stage B --- the hyperbolic readout: harmful, within limits we had to find}
\label{sec:stageB}

Every arm containing Stage B loses $0.75$--$1.24$ Spearman on BERT STS-B, with
$0$ or $1$ of $15$ seeds positive; B alone gives $t=-17.00$ and a sign-test $p$
of $6\times10^{-5}$, the smallest value $15$ paired seeds can produce. All four
survive Bonferroni. It repeats on RoBERTa STS-B at closely matching effect
sizes ($-1.264$ for B against $-1.065$ on BERT, $-0.795$ for AB against
$-0.747$), and on the decoder in a campaign that ran its own baseline at the
same seeds ($-1.16$, $2/13$, $t=-3.53$). On the decoder's CoLA it is far larger:
B costs $10.26$ MCC and AB costs $13.58$, both $0/15$ and both
Bonferroni-significant, the two biggest effects in any of our fine-tuning
tables.

\paragraph{There is a mechanism, not just a mean.} On the decoder's STS-B the
seed standard deviation rises in step with the number of hyperbolic parts:
$0.49 \rightarrow 0.95 \rightarrow 1.87 \rightarrow 3.58$ for baseline, BC, AB
and ABC. The gyro-midpoint behaves like a variance amplifier rather than a
useful prior --- and at the extreme it stops being a variance effect at all: two
of AB's fifteen decoder CoLA seeds collapse to $\mathrm{MCC}=0$, the
majority-class predictor. It is also the stage whose theoretical motivation is
weakest here, since pooling averages tokens the graph has already smoothed and
the Einstein midpoint pulls hardest on exactly the large-norm tokens
Appendix~\ref{app:calib} shows to be outliers.

\paragraph{RTE changes direction with the backbone.} On BERT all four
B-containing arms are positive and none is significant or could be --- that
column's minimum detectable effect, $2.06$ points, exceeds every effect in the
block. On RoBERTa, B and AB are significantly \emph{negative}. On the decoder
all four are positive and BC reaches $+4.33$ with $14/15$ seeds, the only
B-containing arm in our fine-tuning tables significantly better than its
baseline --- though that column's baseline sits at chance
(Appendix~\ref{app:chance}).

\textbf{Verdict: harmful under supervised fine-tuning on small tasks; helpful
or neutral elsewhere.} Two boundaries matter more than the operator. The first
is \emph{data}: SST-2 has $67{,}349$ training examples, $7.9\times$ CoLA, and
there B is $+0.65$ accuracy with $10/5$ seeds --- resolved by the $t$-test, not
by the sign test, so by our own rule a null rather than a gain
(Appendix~\ref{app:sst2}). Its variance amplification shrinks in step, from
$1.98\times$ the baseline standard deviation on decoder STS-B to $1.47\times$.
The second is \emph{regime}: trained contrastively on MS MARCO and evaluated
zero-shot, arms containing this readout produce the largest effects in the paper
in both directions. On BERT, B is positive on two of seven tasks and negative on
four; on RoBERTa the balance inverts to four positive ($+12.80$ on STS12,
$+5.08$ on SciFact, $+4.68$ on SummEval, $+4.29$ on RedditClustering, all
$15/15$) and one negative ($-6.13$, $0/15$), all five surviving Bonferroni. The
claim we are entitled to is about a readout in a regime and at a scale, not
about the gyro-midpoint.

%-------------------------------------------------------------------------
\subsection{Stage C --- hyperbolic message passing: the gain is configuration, not curvature}
\label{sec:stageC}

Stage C is the one arm that looks like a win on CoLA: $+1.42$ MCC against the
BERT baseline, significant on both tests. It is also the arm whose selected
configuration differs from the baseline's in two ways at once --- a
$3.6\times$ denser graph and a network half as deep and half as wide --- so the
contrast confounds curvature with configuration.

Two further asymmetries were ours, not the original's. Our hyperbolic layers
appended a self-loop to every loop the graph builder already emitted, while the
Euclidean control removes existing loops first; and the Euclidean control
consumes an edge attribute the hyperbolic layer ignores, which is not inert
because it enters before a leaky ReLU (Appendix~\ref{app:defects}). Both are
now fixed and verified by a forward-pass test.

\paragraph{The matched factorial.} We reran the full $2\times2$ --- geometry
$\times$ configuration --- at $65$ seeds with parity closed, all four cells in
one campaign (Appendix~\ref{app:factorial2x2}). The total reproduces the arm
level effect at $+1.862$ ($51/14$, $t=6.51$). The decomposition does not
support attributing it to curvature: geometry alone is $+0.502$ and significant
on neither test ($t=1.45$, sign $p=0.136$), configuration alone is $+1.136$ and
significant on both, and the interaction is $+0.224$ at $33/32$ seeds.

Closing the two parity defects moved the geometry term by $0.032$ MCC, from
$+0.534$ to $+0.502$. The confound was real and had to be repaired before the
contrast was admissible; it was not what made Stage C look good.

\textbf{Verdict: Stage C helps on BERT CoLA, and the help is not the manifold.}
On RoBERTa retrieval it is significantly negative on three of seven tasks
(Appendix~\ref{app:mteb3}), and on the decoder's CoLA it costs $2.96$ MCC.

%-------------------------------------------------------------------------
\subsection{How the stages combine does not transfer}
\label{sec:interaction}

Stage B's harm is stable under fine-tuning; what happens when the hyperbolic GNN
is added on top of it is not. Adding C to B rescues the arm in three settings,
worsens it in two, and does nothing in two more (Table~\ref{tab:bxc}). The
extremes are not noise: BC and ABC on RoBERTa CoLA lose $2.16$ and $3.31$ MCC
and both survive Bonferroni, while on the decoder's CoLA adding C to B recovers
over nine of the $10.26$ MCC B costs. So a two-component summary of the form
``B harms, C offsets it'' is not supported. Stage B's harm is a property of the
readout, reproducible across backbones and tasks; the sign of the B$\times$C
interaction is a property of the particular task and backbone, and we cannot
predict it.

\paragraph{The same instability appears on retrieval, with the opposite
resolution.} On BERT, ABC reproduces B's pattern on all seven MTEB tasks and on
the same side of zero --- the readout sets the outcome and the graph and message
passing ride along. On RoBERTa it does not: A alone and C alone are each
significantly negative on several tasks, AC is negative on five, and yet ABC is
positive on six and larger than B alone on every one of them
(Appendix~\ref{app:mteb3}). Stages that hurt in isolation help when stacked on
B. We have no account of why, and it is the sharpest form of the same lesson:
how the stages combine is a property of the regime, not of the operators.

\paragraph{Why the stage-wise design was necessary.} B and C carry opposite
signs, so a study that turned ``hyperbolic'' on as one switch would have
reported their average --- close to zero --- and concluded that geometry does
not matter here. Worse, that average is not a stable quantity: because the sign
of the interaction changes with task and backbone, two studies running the same
all-or-nothing comparison on two backbones could report opposite conclusions
without either being wrong.

%-------------------------------------------------------------------------
\subsection{The premise: measured tree-likeness is an artifact}
\label{sec:premise}

Hyperbolic layers are usually justified by measuring Gromov
$\delta$-hyperbolicity and finding it small. On the Euclidean metric ModernBERT
at layer $16$ gives $\delta = 0.0021$, which reads as almost perfectly
tree-like. But that layer has a maximum-to-median token norm ratio of $156$, and
$\delta$ is scale-sensitive: a few very large ``attention sink'' tokens dominate
it and pull it toward zero --- a star graph is a tree. On the angular metric
$\arccos(\cos(\cdot,\cdot))$, which ignores those norms, ModernBERT gives
$0.2127$ and BERT gives $0.2114$: indistinguishable, while the Euclidean numbers
differ by two orders of magnitude. Flan-T5 shows the same pattern with
$357\times$ norm outliers. So measured tree-likeness cannot tell you which
backbone or which component will benefit from curvature. We did not use it to
choose; we applied all three stages everywhere and let the paired tests decide.

%-------------------------------------------------------------------------
\subsection{Porting the method, and the scope of these claims}
\label{sec:porting}

Equation~\ref{eq:adj} compares a cosine to a fixed number, which only makes
sense if cosine scale is comparable across backbones. It is not: on RoBERTa the
\emph{tenth}-percentile token cosine is $0.693$, above the largest $\tau$ in the
published grid, so every searched setting yields an essentially complete graph
($0.991$ density). The useful finding is what happens next. \textsc{Glot} is
unharmed by that dense graph, while the obvious repair --- switching to a
quantile threshold --- \emph{costs} $1.41$ MCC ($2/13$). We therefore withdraw
density-matching as a recommendation for anyone porting this method
(Appendix~\ref{app:calib}).

Deleting the token graph outright, meanwhile, costs nothing measurable on GLUE
on either encoder, and on BERT is mildly positive ($+0.83$ MCC on CoLA, $11/4$).
We do not read that as a verdict on \textsc{Glot} --- \texttt{no graph} is still
a trained pooler, and the graph does beat real bag-of-tokens poolers by
$14$--$35$ points on the noise diagnostic (Appendix~\ref{app:stress}) --- but it
is the bar any geometry claim has to clear first, and Stage C clears it on BERT
STS-B and fails it on BERT CoLA.

\paragraph{Scope.} We report four GLUE tasks with the full nine-arm design on
BERT-base \citep{devlin2019bert}, four on RoBERTa \citep{liu2019roberta}, three
on TinyLlama \citep{zhang2024tinyllama}, a four-arm reduced design on SST-2, a
synthetic noise diagnostic, IMDB, and the original's seven MTEB tasks on both
encoders; \citet{mantri2026glot} report ten GLUE tasks on
six backbones. The four we chose are the four \emph{smallest}, and not by
accident: our design costs $495$ runs per task
(Appendix~\ref{app:limitations}).

%-------------------------------------------------------------------------
\section{Related work}
\label{sec:related}

Hyperbolic layers descend from Poincar\'e embeddings of hierarchies
\citep{nickel2017poincare,nickel2018lorentz} and from the gyrovector calculus
that turned them into trainable layers
\citep{ganea2018hyperbolic,shimizu2021hnnpp}. Our Stage C is exactly that
construction, and its HGCN and HGAT variants are the two standard neighbourhood
aggregations \citep{chami2019hgcn,zhang2019hgat}. Stage B is older than either:
the Einstein midpoint was adopted for attention precisely because the
Fr\'echet mean has no closed form \citep{gulcehre2018hyperbolicattention}.
\emph{What none of this literature does is vary its geometric components
independently inside one architecture} --- each paper proposes a full hyperbolic
model and compares it against a full Euclidean one. Our result is that on
\textsc{Glot} those components disagree in sign, so the aggregate comparison
every one of them runs would have reported their average.

The usual empirical warrant for going hyperbolic is a small Gromov $\delta$,
measured on the data and used to choose curvature
\citep{khrulkov2020hyperbolicimage,tifrea2018poincareglove}; \S\ref{sec:premise}
contradicts that premise rather than the method, because $\delta$ on raw
activations is dominated by attention sinks and massive activations
\citep{xiao2024attentionsinks,sun2024massiveactivations}.

Our protocol answers a long line of work showing that architectural comparisons
in ML are dominated by search budget and seed variance rather than by the
architecture
\citep{reimers2017scoredistributions,melis2018stateoftheart,lucic2018gans,musgrave2020realitycheck,narang2021modifications},
and in particular by the biased maximum over $T$ trials
\citep{dodge2019showyourwork}. The strongest objection to our own negative
result comes from the same literature: \citet{qiu2024computebetterspent} show
that structured replacements for dense layers underperform under dense-tuned
learning rates. Appendix~\ref{app:related} gives the full discussion, including
where \textsc{Glot} itself sits among sentence poolers.

%-------------------------------------------------------------------------
\section{Conclusion}

Hyperbolic substitutions in graph-based pooling have conditional effects. Their
sign changes with the stage, task, backbone, training regime and dataset size,
by margins up to $+17.1$ Spearman and $-15.5$ max-AP. A descriptive tally of
individually resolved comparisons is heterogeneous and is not evidence that
geometry has a single direction of effect. The useful question is which regime
produces which effect, not whether curvature works in general.

Stage B, the gyro-midpoint readout, is reliably harmful under supervised
fine-tuning on small tasks --- negative in every setting with the power to
measure it, surviving Bonferroni on two encoders and a decoder, costing $10.3$
MCC on the decoder's CoLA, with a variance signature that suggests why. Both
boundaries of that claim turned out to matter: at $67{,}349$ training examples
the harm disappears, and trained contrastively and evaluated zero-shot the same
readout appears in the arms producing the largest positive effects we measured.
Stage C's apparent CoLA gain survives a matched factorial at $65$ seeds but
decomposes into configuration rather than curvature. Stage A separates from the
baseline on CoLA at $50$ seeds and is significantly negative on six of seven
RoBERTa retrieval tasks.

Three lessons travel beyond this architecture. The standard reason to go
hyperbolic --- a low measured $\delta$ --- is an artifact of norm outliers, so
measured tree-likeness cannot tell you where curvature will help. A comparison
between geometries is only as good as the budget given to its control: our own
retracted result and the confound in our headline number both come from arms
that were not given the same freedom. And a seed is not a run: unless
deterministic kernels are requested, re-running one configuration at one seed
can move a metric further than the effect being measured, which silently
invalidates any table that splices cells from two campaigns.

\subsubsection*{Reproducibility statement}

Every run appends a row to a per-campaign CSV recording the configuration, the
realised edge density, the per-epoch metrics and the wall-clock time. These logs
are released unmodified, including runs that crashed or produced empty graphs,
and the analysis scripts behind every table read those CSVs directly. Caches are built before any arm runs; that
ordering is load-bearing, not an optimisation (\S\ref{sec:choices}). We do
\emph{not} claim bit-level reproducibility: Appendix~\ref{app:determinism}
measures how far a re-run of the same configuration at the same seed can move,
and the answer is far enough to matter. One environment caveat belongs here
rather than in a footnote: the manifold library that supplies the exponential
and logarithmic maps for all three stages was not version-pinned for most of
this project. No comparison in this paper crosses a machine or environment
boundary --- every paired test is within a single campaign on a single machine
--- but a reproducer starting from our \texttt{requirements.txt}, which now pins
it, may not land on the same absolute numbers.

\label{page:endprose}

\bibliography{references}
\bibliographystyle{iclr2026_conference}

%=========================================================================
\appendix
\label{page:startappendix}

\section{Related work in full}
\label{app:related}

\paragraph{Hyperbolic layers, and where our three stages come from.} Hyperbolic
space embeds trees with far less distortion than Euclidean space, which
motivated Poincar\'e embeddings of hierarchies \citep{nickel2017poincare} and
their Lorentz-model successor \citep{nickel2018lorentz}. Turning those
embeddings into layers required a way to do linear algebra on a manifold:
\citet{ganea2018hyperbolic} supplied it, deriving M\"obius linear maps,
logistic regression and recurrent cells from gyrovector formalism, and
\citet{shimizu2021hnnpp} later unified the same components with fewer
parameters. Our Stage C's $W \otimes_c h = \exp^c_0(W\log^c_0 h)$ is exactly
that construction. Aggregation over a \emph{neighbourhood} came next, in two
flavours we reuse directly: degree-normalised tangent-space aggregation
\citep{chami2019hgcn,liu2019hgnn} and attention-weighted aggregation
\citep{zhang2019hgat}, which are our HGCN and HGAT variants. Stage B is older
than either. \citet{gulcehre2018hyperbolicattention} re-expressed soft attention
in the Klein model and aggregated with the Einstein midpoint precisely because
the Fr\'echet mean has no closed form; \citet{chamberlain2019scalable} scaled
the same midpoint to production recommendation. In language specifically,
hyperbolic word embeddings \citep{tifrea2018poincareglove} and, recently,
billion-parameter hyperbolic language models \citep{he2025helm} report gains
over Euclidean counterparts.

\paragraph{Measured tree-likeness as a justification.} The standard empirical
warrant for going hyperbolic is a small Gromov $\delta$, computed on the data
with the four-point condition, at a cost of $O(n^{3.69})$ for an exact answer
\citep{fournier2015gromov}. \citet{khrulkov2020hyperbolicimage} made this the
explicit design step --- measure $\delta_{\text{rel}}$ on a dataset's
embeddings, then choose curvature accordingly --- and
\citet{tifrea2018poincareglove} justify hyperbolic word embeddings the same
way. Our measurement in \S\ref{sec:premise} contradicts the premise rather than
the method: $\delta$ on the Euclidean metric is dominated by a handful of tokens
with enormous norms, and such tokens are now a well-documented and
\emph{architectural} feature of transformers --- attention sinks
\citep{xiao2024attentionsinks} and massive activations
\citep{sun2024massiveactivations}, whose values are largely input-independent.
On an angular metric that ignores them, the backbones $\delta$ is used to tell
apart become indistinguishable. The same outlier and anisotropy structure
\citep{ethayarajh2019contextual,gao2019degeneration} is what breaks the
\emph{cosine} threshold when \textsc{Glot} is ported across backbones
(Appendix~\ref{app:calib}). The closest prior scepticism about curvature is
\citet{kochurov2020createdequal}, who find Euclidean models superior on several
graph tasks; they compare whole models on graph benchmarks, whereas we hold one
architecture fixed and vary its parts.

\paragraph{Sentence pooling.} Most sentence encoders reduce token states with an
order-free operator and put their effort into the training objective, as with
siamese fine-tuning \citep{reimers2019sbert} or contrastive learning
\citep{gao2021simcse}. A separate line builds an explicit graph over text and
runs a GNN on it, classically over a corpus-level word--document graph
\citep{yao2019textgcn}. \textsc{Glot} \citep{mantri2026glot} is the
sentence-level, frozen-backbone instance of that idea, and the pooler we study;
its components --- graph attention \citep{velickovic2018gat} and
jumping-knowledge aggregation across layers \citep{xu2018jk} --- are standard,
and we also test GCN \citep{kipf2017gcn} and GIN \citep{xu2019gin} in its place
(Appendix~\ref{app:factorial}).

\paragraph{Why the protocol is half of this paper.} \citet{reimers2017scoredistributions}
showed that the seed alone moves sequence taggers by a full F1 point;
\citet{melis2018stateoftheart} and \citet{lucic2018gans} found that carefully
tuned baselines erase claimed advances in language modelling and in GANs;
\citet{musgrave2020realitycheck} reached the same verdict for metric learning;
and \citet{narang2021modifications} found that most Transformer modifications do
not transfer at all. \citet{dodge2019showyourwork} give the concrete mechanism
we hit twice --- the maximum over $T$ trials is a biased estimate that reorders
methods as $T$ changes --- and equal-budget random search
\citep{bergstra2012randomsearch} is our response to it. Our retracted Stage A
result (Appendix~\ref{app:narrow}) is a textbook instance. What this literature
does not cover, and what Appendix~\ref{app:determinism} adds, is that on GPU the
seed itself may not identify a run, so the variance pairing is supposed to
remove can be irreducible. \citet{qiu2024computebetterspent} show that
structured replacements for dense layers ``significantly underperform'' under
dense-tuned learning rates and that their benefit appears only with
structure-aware initialisation and step sizes. Our search moved the learning
rate per arm and per task, but we did not implement Riemannian optimisation or
curvature-aware initialisation, so that objection stands against Stage B and we
record it as such (Appendix~\ref{app:limitations}).

\section{Hyperbolic preliminaries}
\label{app:math}

We work in the Poincar\'e ball
$\mathbb{B}^d_c = \{x \in \mathbb{R}^d : c\|x\|^2 < 1\}$ of curvature $-c$,
with M\"obius addition $\oplus_c$ and the maps
\begin{equation}
\exp^c_0(v) = \tanh\!\big(\sqrt{c}\,\|v\|\big)\frac{v}{\sqrt{c}\,\|v\|},
\qquad
\log^c_0(x) = \operatorname{artanh}\!\big(\sqrt{c}\,\|x\|\big)\frac{x}{\sqrt{c}\,\|x\|},
\end{equation}
and geodesic distance
$d_c(x,y) = \tfrac{2}{\sqrt{c}}\operatorname{artanh}\big(\sqrt{c}\,\|{-}x \oplus_c y\|\big)$.
With features rescaled to unit mean norm, $c = r_{\text{eff}}^2$.

Stage A joins tokens when
$A_{ij} = \mathbf{1}[\,d_c(\exp^c_0(\tilde{x}_i), \exp^c_0(\tilde{x}_j)) < \rho_q\,]$,
with $\rho_q$ the $q$-quantile of all pairwise distances. Stage B replaces the
weighted-sum readout with the Einstein midpoint in Klein coordinates,
\begin{equation}
m_K = \frac{\sum_i a_i \gamma_{k_i} k_i}{\sum_i a_i \gamma_{k_i}},
\qquad k = \frac{2x}{1 + c\|x\|^2},
\qquad \gamma_k = (1 - c\|k\|^2)^{-1/2}.
\label{eq:midpoint}
\end{equation}
Stage C replaces each GNN layer with
\begin{equation}
h_i' = \exp^c_0\!\Big(\sigma\Big(\textstyle\sum_{j \in \mathcal{N}(i)}
       \alpha_{ij}\, \log^c_0\big(W \otimes_c h_j\big)\Big)\Big),
\end{equation}
with $W \otimes_c h = \exp^c_0(W \log^c_0(h))$ and $\alpha_{ij}$ either
degree-based (HGCN) or attention (HGAT).

\section{Architecture}
\label{app:fig}

\begin{figure}[h]
\begin{center}
\includegraphics[width=\textwidth]{hyperglot_architecture}
\end{center}
\caption{Stage-wise hyperbolic substitutions in graph-based token pooling. A
frozen backbone produces token states, which are converted to a sparse token
graph, refined by token-level message passing, and aggregated into one sentence
vector. Stage A replaces cosine graph construction with Poincar\'e distance;
Stage C replaces Euclidean message passing with M\"obius message passing; and
Stage B replaces the Euclidean attention-weighted sum with an Einstein
gyro-midpoint. Each stage can be enabled independently or jointly; only the
pooling head and task head train.}
\label{fig:overview}
\end{figure}

\section{Protocol}
\label{app:protocol}

Frozen backbone, $2$ epochs, Adam, weight decay searched, batch size $32$; the
pooler is the only trained component. Confirmation uses seeds $\{1,\dots,15\}$
shared across arms; tuning uses one disjoint held-out seed.

\paragraph{Three seed budgets, and which is which.} Two contrasts were run at
larger budgets, and say so wherever they appear: the Stage A re-test on CoLA at
$n=50$ (\S\ref{sec:stageA}) and the Stage C factorial at $n=65$
(Appendix~\ref{app:factorial2x2}). Both were commissioned in advance to settle a
specific question, not selected after the fact for looking better at a larger
$n$. Everything in Tables~\ref{tab:absolute}, \ref{tab:verdict},
\ref{tab:mteb3}, \ref{tab:mteb3rob} and~\ref{tab:sst2} is $n=15$; the
diagnostics elsewhere in the appendix use fewer seeds and state their own $n$.

\paragraph{Matched budget.} Trials per arm are $40$ in the BERT wide campaigns
and on RoBERTa, and $10$ on the decoder and in the earlier graph-only campaigns.
Every arm inside a given campaign gets the same number, which is the property
the paired tests actually need. The decoder gets fewer because a TinyLlama run
costs roughly $4\times$ a BERT run; its arms were therefore selected from a
coarser search, so its entries should be read as paired deltas within that
campaign and not compared against the BERT block. The cosine baseline has a
smaller native space, so we widen its grid to keep selection pressure equal.
Equal trials is not equal \emph{coverage}: $40$ trials cover $6.2\%$ of a
$648$-point space but under $0.01\%$ of ABC's $4.98\times10^{7}$-point space.
This asymmetry works against the complex arms, and we note it as a caveat
against our own negative results rather than in support of them.

\paragraph{Part of the grid cannot train, and almost only on one task.} Pooled
over every campaign with a searched learning rate ($2{,}320$ de-duplicated
tuning trials), $24.3\%$ of trials drawn at $\mathrm{lr}=2\times10^{-5}$ score
zero or below, against $3.5\%$ at $2\times10^{-4}$ and $0.1\%$ at
$1\times10^{-3}$. The effect is almost entirely CoLA's: on BERT/CoLA, $81$ of
$121$ trials at that rate ($66.9\%$) collapse to $\mathrm{MCC}=0$ --- the
classifier predicts the majority class --- as do $14$ of $120$ at
$2\times10^{-4}$, so $26\%$ of that task's nominal $40$-trial budget buys
nothing. RoBERTa/CoLA behaves the same way and slightly worse ($93$ of $130$,
$71.5\%$), while MRPC sits at $2.3\%$ and STS-B and RTE at exactly $0\%$ on both
backbones. Every arm draws from the same grid, so no comparison is biased by
this; but the effective search on CoLA is roughly a quarter smaller than the
headline number, and that is the task where our one positive result lives.

\paragraph{Testing.} We report the exact two-sided sign test beside the paired
$t$-test because evaluation-set quantisation makes the parametric standard error
look too small (MRPC accuracy moves in steps of $0.245$ on $408$ examples). Both
tests must agree. Bonferroni is applied over the arms tested against a common
baseline \emph{within} a campaign: $8$ arms on the BERT campaigns, giving
$\alpha = 0.0063$; $10$ on RoBERTa, where the seven hyperbolic arms share a
baseline with the three calibration arms, giving $\alpha = 0.005$; and $8$ on
the decoder once its B and C arms landed, giving $\alpha = 0.00625$ --- though
those two arms are a separate campaign, so that last count is the conservative
reading rather than the one our own rule strictly implies
(Appendix~\ref{app:decoder}). The count
is a property of the campaign, so adding arms to a campaign tightens the
threshold for arms already in it --- which it has now done twice
(Appendix~\ref{app:calib}, \S\ref{sec:stageB}). Minimum detectable effects at
$n=15$, from the median paired standard error actually observed, are $0.225$
(BERT STS-B), $1.153$ (BERT CoLA), $0.317$ (MRPC), $2.064$ (RTE), $1.000$
(RoBERTa CoLA), $0.243$ (RoBERTa STS-B), $0.503$ (TinyLlama STS-B), $2.961$
(TinyLlama CoLA) and $0.431$ (SST-2). RTE's figure
exceeds every effect in this paper, so that task can only ever return a null; we
report it, and draw nothing from it.

\section{The choices that changed the answer}
\label{app:choices}

The first of the six is in \S\ref{sec:choices} and
Appendix~\ref{app:determinism}; the other five are here.

\paragraph{The search space must be the same for every arm.} An earlier
campaign of ours searched only graph knobs and left the learning rate, depth and
width pinned at values picked for a \emph{flat} pooler. That handed the baseline
an advantage and produced a false positive we retract in
Appendix~\ref{app:narrow}: Stage A on STS-B looked like $+0.223$ Spearman with
$15/15$ seeds and $t(14)=6.20$, and collapsed to $+0.028$ once the baseline was
allowed to pick $K=4$. Every BERT and RoBERTa campaign here uses \textsc{Glot}'s
full grid applied identically to all arms: $40$ trials each, $15$ seeds, $495$
runs per task on BERT and $1{,}980$ across the four GLUE tasks. What has to be
equal is the budget \emph{within} a campaign, not the same number everywhere ---
the decoder gave every arm $10$ trials rather than $40$, for cost.

\paragraph{The search really does use that freedom.} The learning rate does not
settle on one value. On STS-B all nine arms chose $2\times10^{-4}$; on CoLA
eight of nine chose $1\times10^{-3}$; MRPC and RTE split. Depth, width and
threshold move too. Arms were therefore not locked to a rate chosen for a flat
pooler, which is the main form of the objection that non-Euclidean layers get
judged under flat-tuned optimisers. Appendix~\ref{app:hp} lists the selected
configurations.

\paragraph{Tuning maxima are inflated, and can reverse a ranking.} With $T$
trials and per-seed noise $\sigma$, the best of $T$ sits above the truth even
for an arm identical to the baseline --- about $1.83$ here on STS-B. On the
RoBERTa campaign the inflation is large enough to flip the order outright: the
quantile correction is first of five at the tuning maximum ($56.77$) and last
of five over $15$ seeds ($53.19$). Reporting tuning maxima would have inverted
our conclusion. We report confirmation means only.

\paragraph{Cache order is worth about $5$ MCC.} The released code caches
backbone states and builds the shuffled loader \emph{before} the classifier is
created. On a cache miss the loader consumes the global RNG; on a hit it does
not. The same seed therefore gives a different initialisation depending on cache
state --- $40.37$ cold against $45.54$ warm on CoLA, about six times the seed
standard deviation. We build all caches before any arm runs. Any reproduction
that skips this will shift by several points.

\paragraph{The estimator matters as much as the method.} \citet{mantri2026glot}
fix seed $42$ and report the best $\tau$ per task on the split they also report,
so every published number is one draw maximised over a search. We report
$15$-seed means. On CoLA the choice of estimator alone moves the same runs by
nearly $5$ MCC (Appendix~\ref{app:repro}). Matching their estimator exactly, we
reach $50.52$ MCC on CoLA against a published $47.49$ and $59.57$ on RTE against
$59.21$; STS-B stays $0.87$ short and we could not explain it. Every comparison
in this paper is against our own baseline under identical conditions, never
against a published number.

\section{A seed does not pin a run}
\label{app:determinism}

\texttt{set\_seed} in the released code seeds \texttt{random}, \texttt{numpy},
\texttt{torch} and \texttt{torch.cuda}. It does not call
\texttt{torch.use\_deterministic\_algorithms}, does not set
\texttt{cudnn.deterministic}, and does not set
\texttt{CUBLAS\_WORKSPACE\_CONFIG}. Scatter-based neighbourhood aggregation uses
atomic adds on CUDA, whose summation order is not fixed, so two runs of the same
configuration at the same seed diverge from the first backward pass and the
divergence is amplified over two epochs of training.

We measured it by accident. The extended Stage C factorial re-runs the baseline
cell itself rather than borrowing it, and its configuration string is
character-for-character identical to the one recorded for the \texttt{baseline}
arm of the main CoLA campaign. Over the $15$ shared seeds:

\begin{center}
\small
\begin{tabular}{lc}
\toprule
Statistic of (factorial re-run $-$ original), CoLA MCC & value \\
\midrule
seeds where the two agree exactly & $0$ of $15$ \\
mean difference & $+0.334$ \\
standard deviation of the difference & $1.472$ \\
largest single-seed difference & $3.61$ \\
\midrule
for comparison: the effect this campaign reports for Stage C & $+1.416$ \\
\bottomrule
\end{tabular}
\end{center}

Two consequences, one benign and one not.

\paragraph{Benign: within-campaign paired tests are still valid.} Both arms of
every paired test in this paper were run in the same campaign, once each. Re-run
non-determinism is part of the within-pair scatter that the paired $t$ and sign
tests already estimate from the data; it inflates the standard error rather than
biasing the difference. Nothing in
Tables~\ref{tab:absolute}--\ref{tab:verdict} needs to change.

\paragraph{Not benign: pairing recovers less than we claimed, unevenly.} On
BERT STS-B the paired difference for arm B has $\mathrm{sd}=0.243$ against an
independent-samples estimate of $0.489$ --- pairing halves the variance, as
intended. On BERT CoLA the paired difference for arm C has $\mathrm{sd}=2.08$
against an independent-samples estimate of $1.52$: pairing is \emph{worse} than
not pairing, which means the seed explains essentially none of CoLA's variance
and the run-to-run term dominates. Statements of the form ``every comparison is
paired, so the dominant variance component cancels'' are true on STS-B and false
on CoLA.

\paragraph{And: cells may not be spliced across campaigns.} Our own $2\times2$
in Table~\ref{tab:factorial} originally took two cells from the wide campaign
and ran only the two off-diagonal ones. Substituting a re-run of the shared cell
moved the geometry component of Stage C's gain from $+0.40$ to $+0.07$; the
$65$-seed job, which runs all four cells through one script, one code path and
one cache state, puts it at $+0.502$ (Table~\ref{tab:decomp}). The three
numbers span an order of magnitude and differ only in how the shared cell was
obtained, which is the whole argument for never splicing. The one place we were
able to check the size of the error directly --- the decoder's STS-B B and C
arms, rerun in a matched campaign --- the splice turned out to be accurate to two
decimals (Appendix~\ref{app:decoder}). That is consistent rather than
contradictory: the run-to-run term dominates on CoLA and is small on STS-B, so
splicing fails exactly where the pairing analysis above says it should.

\section{Selected hyper-parameters}
\label{app:hp}

Each arm tunes its own configuration; these are the winners then run on all $15$
confirmation seeds. Two things are worth reading off Table~\ref{tab:hp}. The
learning rate does not converge to a single value across tasks, so no arm was
pinned to a rate chosen for a flat pooler. And on CoLA, Stage C selected a much
denser graph ($q = 0.38$) and a smaller network than the baseline ($q = 0.10$,
twice the depth, twice the width) --- the confound discussed in
\S\ref{sec:stageC}. On MRPC the same search went the other way, giving C a
$15\times$ \emph{sparser} graph than the baseline; C is negative there. C wins
where it picked dense and loses where it picked sparse, which is why the matched
comparison is necessary.

\begin{table}[h]
\caption{Confirmed configurations, BERT wide campaigns. ``$q$'' is
\texttt{tau\_quantile} for cosine arms and \texttt{rho\_quantile} for Poincar\'e
arms; both mean ``keep the closest $q$ fraction of token pairs''. ``width'' is
the GNN hidden dimension, ``proj'' the projection dimension.}
\label{tab:hp}
\begin{center}
\small
\begin{tabular}{llccccc}
\toprule
Task & Arm & lr & layers & width & proj & $q$ \\
\midrule
\multirow{5}{*}{CoLA}
 & baseline & $1{\times}10^{-3}$ & 4 & 256 & 128 & 0.10 \\
 & A        & $1{\times}10^{-3}$ & 4 & 256 & 256 & 0.05 \\
 & B        & $2{\times}10^{-4}$ & 2 & 256 & 512 & 0.38 \\
 & C        & $1{\times}10^{-3}$ & 2 & 128 & 256 & \textbf{0.38} \\
 & AC       & $1{\times}10^{-3}$ & 2 & 128 & 256 & 0.05 \\
\midrule
\multirow{5}{*}{STS-B}
 & baseline & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 256 & 0.20 \\
 & A        & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 256 & 0.10 \\
 & B        & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 128 & 0.38 \\
 & C        & $2{\times}10^{-4}$ & 2 & 128 & 128 & 0.20 \\
 & AC       & $2{\times}10^{-4}$ & 4 & 128 & 512 & 0.20 \\
\midrule
\multirow{3}{*}{MRPC}
 & baseline & $2{\times}10^{-4}$ & 2 & 128 & 128 & 0.38 \\
 & B        & $1{\times}10^{-3}$ & 4 & 128 & 256 & 0.10 \\
 & C        & $2{\times}10^{-4}$ & 2 & 128 & 256 & \textbf{0.025} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Other selected settings vary similarly. Jumping-knowledge mode is uniform across
all nine arms on CoLA (\texttt{max}) and on STS-B (\texttt{cat}), and
near-uniform on MRPC and RTE, where exactly one arm each departs from the
majority --- so it tracks the task far more than the arm. Curvature, where it
applies, spans the whole searched range $\{0.25, 1.0, 4.0\}$ with no consistent
preference.

\section{Reproduction}
\label{app:repro}

\begin{table}[h]
\caption{One method, four estimators, on CoLA. Each row is the \emph{same}
pooler; only the way the score is computed changes. A dash means the knob does
not apply. The published value sits between our mean and our maximum, and the
row that matches how it was computed exceeds it.}
\label{tab:estimator}
\begin{center}
\small
\begin{tabular}{lllc}
\toprule
Seeds & Aggregated & Selected over & CoLA MCC \\
\midrule
\multicolumn{4}{l}{\emph{Published \citep{mantri2026glot}}} \\
1 (seed 42) & --- & best $\tau$ of 3 & 47.49 \\
\midrule
\multicolumn{4}{l}{\emph{Ours, same method}} \\
15 & mean & --- & 45.37 \\
15 & max  & --- & 48.17 \\
1 (seed 42) & --- & best of 40 configs & \textbf{50.16} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{Matched-estimator comparison: best score at \textsc{Glot}'s own seed
($42$) over \textsc{Glot}'s own grid, $68$ configurations per task, so both
columns are computed the same way. The selected configuration is given so the
row can be re-run; $K$ is depth, $h$ the GNN width.}
\label{tab:repro}
\begin{center}
\small
\begin{tabular}{lcccl}
\toprule
Task & ours & published & $\Delta$ & selected configuration \\
\midrule
CoLA (MCC)       & \textbf{50.52} & 47.49 & $+3.03$ & $\tau{=}0.3$, $K{=}4$, $h{=}256$, lr $10^{-3}$, jk\,=\,max \\
RTE (acc.)       & \textbf{59.57} & 59.21 & $+0.36$ & $\tau{=}0.1$, $K{=}4$, $h{=}128$, lr $10^{-3}$, jk\,=\,max \\
STS-B (Spearman) & 82.99 & 83.86 & $-0.87$ & $\tau{=}0.3$, $K{=}4$, $h{=}64$, lr $2{\times}10^{-4}$, jk\,=\,cat \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The published $\tau$-sensitivity curve (their Table 8) cannot be read as a
$\tau$ effect. On CoLA it spans $7.87$ MCC ($39.62 \rightarrow 47.49$) and on
RTE $8.31$ ($50.90 \rightarrow 59.21$), but we measure a spread of $5.46$ across
seeds at \emph{fixed} $\tau$ on CoLA ($42.71 \rightarrow 48.17$), and $11.92$
across the $68$ configurations at \emph{fixed} seed $42$ on RTE --- larger than
the published $\tau$ range it is meant to explain. Both published rows are flat
with a single spike, and in each case that spike is the value promoted to their
Table~1.

\section{Main results}
\label{app:results}

Table~\ref{tab:absolute} gives the absolute scores behind every delta in this
paper. It follows the layout of Table 1 of \citet{mantri2026glot} --- backbone
blocks, method rows, task columns, scores $\times100$, best per column in bold,
metric row under the header --- but the rows are our arms rather than competing
poolers, because our question is which \emph{component} of \textsc{Glot} does
what, not whether \textsc{Glot} beats mean-pooling. The four bag-of-tokens rows
of the original (\texttt{[CLS]}/\texttt{[EOS]}, Mean, Max, AdaPool) are not
filled: we never ran them on GLUE. Those poolers appear only in the noise
diagnostic (Appendix~\ref{app:stress}), where they lose by $14$--$35$ points.

\begin{table}[h]
\caption{Absolute confirmed scores, mean $\pm$ sd over the $15$ shared seeds,
$\times100$. Metrics follow \citet{mantri2026glot}: MCC for CoLA, Spearman for
STS-B, F1 for MRPC, accuracy for RTE. Best per column within a backbone block in
bold. \texttt{no graph} deletes the token graph entirely and is an ablation of
\textsc{Glot} itself, not one of our stages; \texttt{published} $\tau$ and
\texttt{quantile} $\tau$ are RoBERTa threshold-calibration arms
(Appendix~\ref{app:calib}). Every number is a confirmation mean, never a tuning
maximum. A dash is a cell we do not intend to run (Appendix~\ref{app:limitations}).
The decoder's B and C on STS-B come from a second campaign that re-ran its own
baseline at the same $15$ seeds ($79.94 \pm 0.52$, against the main campaign's
$79.95 \pm 0.49$); their deltas in Table~\ref{tab:verdict} are taken within that
campaign (Appendix~\ref{app:decoder}).}
\label{tab:absolute}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
Backbone & Method & CoLA & STS-B & MRPC & RTE \\
         &        & MCC $\uparrow$ & Spear. $\uparrow$ & F1 $\uparrow$ & Acc. $\uparrow$ \\
\midrule
\multirow{9}{*}{BERT-base}
 & \textsc{Glot} (baseline) & 45.37\,$\pm$1.30 & 82.53\,$\pm$0.33 & 82.14\,$\pm$0.44 & 53.79\,$\pm$3.36 \\
 & A                        & 46.07\,$\pm$0.99 & 82.56\,$\pm$0.31 & \textbf{82.18}\,$\pm$0.29 & 53.82\,$\pm$1.58 \\
 & B                        & 44.11\,$\pm$1.48 & 81.47\,$\pm$0.36 & 81.81\,$\pm$0.39 & 54.78\,$\pm$1.58 \\
 & C                        & \textbf{46.78}\,$\pm$0.79 & 82.73\,$\pm$0.31 & 81.83\,$\pm$0.39 & 54.35\,$\pm$2.07 \\
 & AB                       & 44.13\,$\pm$1.61 & 81.79\,$\pm$0.46 & 81.86\,$\pm$0.41 & 54.59\,$\pm$2.93 \\
 & AC                       & 45.82\,$\pm$1.94 & \textbf{82.77}\,$\pm$0.20 & 81.99\,$\pm$0.35 & 55.26\,$\pm$1.80 \\
 & BC                       & 45.59\,$\pm$1.19 & 81.29\,$\pm$0.32 & 81.83\,$\pm$0.43 & \textbf{55.64}\,$\pm$1.88 \\
 & ABC                      & 44.84\,$\pm$1.87 & 81.70\,$\pm$0.34 & 81.88\,$\pm$0.44 & 54.85\,$\pm$1.52 \\
 & \texttt{no graph}        & 46.20\,$\pm$2.54 & 82.57\,$\pm$0.34 & 82.17\,$\pm$0.31 & 53.86\,$\pm$2.74 \\
\midrule
\multirow{11}{*}{RoBERTa-base}
 & \textsc{Glot} (baseline) & 54.60\,$\pm$1.52 & 83.89\,$\pm$0.30 & 82.31\,$\pm$0.17 & 55.86\,$\pm$1.77 \\
 & A                        & 54.37\,$\pm$0.89 & 83.80\,$\pm$0.30 & 82.27\,$\pm$0.25 & 55.91\,$\pm$1.81 \\
 & B                        & 54.23\,$\pm$1.64 & 82.62\,$\pm$0.41 & 82.19\,$\pm$0.27 & 54.18\,$\pm$1.88 \\
 & C                        & \textbf{55.25}\,$\pm$1.39 & 84.18\,$\pm$0.40 & 82.02\,$\pm$0.34 & 53.17\,$\pm$3.57 \\
 & AB                       & 53.89\,$\pm$1.71 & 83.09\,$\pm$0.43 & \textbf{82.35}\,$\pm$0.29 & 54.32\,$\pm$2.22 \\
 & AC                       & 54.60\,$\pm$1.31 & 83.95\,$\pm$0.28 & 82.34\,$\pm$0.24 & \textbf{56.85}\,$\pm$2.87 \\
 & BC                       & 52.44\,$\pm$1.53 & 83.63\,$\pm$0.41 & 82.32\,$\pm$0.26 & 54.22\,$\pm$3.55 \\
 & ABC                      & 51.29\,$\pm$1.83 & 83.75\,$\pm$0.30 & 82.25\,$\pm$0.20 & 56.75\,$\pm$1.43 \\
 & \texttt{no graph}        & 54.32\,$\pm$2.06 & 83.84\,$\pm$0.32 & 82.28\,$\pm$0.23 & 55.69\,$\pm$2.19 \\
 & published $\tau$         & 54.46\,$\pm$1.18 & \textbf{84.21}\,$\pm$0.29 & --- & --- \\
 & quantile $\tau$          & 53.19\,$\pm$1.59 & 83.73\,$\pm$0.29 & --- & --- \\
\midrule
\multirow{9}{*}{TinyLlama-1.1B}
 & \textsc{Glot} (baseline) & 42.28\,$\pm$3.59 & 79.95\,$\pm$0.49 & --- & 52.88\,$\pm$2.92 \\
 & A                        & 43.41\,$\pm$1.84 & 79.99\,$\pm$0.51 & --- & 54.42\,$\pm$2.29 \\
 & B                        & 32.02\,$\pm$6.06 & 78.78\,$\pm$1.03 & --- & 55.86\,$\pm$2.88 \\
 & C                        & 39.32\,$\pm$2.37 & \textbf{80.24}\,$\pm$0.52 & --- & 52.49\,$\pm$2.56 \\
 & AB                       & 28.71\,$\pm$12.30 & 76.39\,$\pm$1.87 & --- & 54.87\,$\pm$3.00 \\
 & AC                       & 44.71\,$\pm$2.88 & 80.15\,$\pm$0.54 & --- & 57.21\,$\pm$1.57 \\
 & BC                       & 41.23\,$\pm$2.79 & 78.78\,$\pm$0.95 & --- & \textbf{57.21}\,$\pm$1.70 \\
 & ABC                      & 37.50\,$\pm$3.85 & 75.44\,$\pm$3.58 & --- & 55.43\,$\pm$2.96 \\
 & \texttt{no graph}        & \textbf{44.86}\,$\pm$2.18 & 79.95\,$\pm$0.48 & --- & 52.44\,$\pm$2.89 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Two things are visible here that the delta tables hide. The whole spread across
the nine arms is $0.37$ F1 on BERT MRPC and $0.33$ on RoBERTa MRPC, and on BERT
RTE the seed standard deviation ($1.5$--$3.4$) is larger than every gap in the
column --- which is why we treat both as null. And within the BERT block the
bolded best cell names four \emph{different} arms across four columns, which is
what a table of noise looks like.

\begin{table}[h]
\caption{Per-stage verdict: paired mean differences against each backbone's own
\textsc{Glot} baseline on shared seeds, with the seed split positive/negative.
$\dagger$ significant on both the paired $t$-test and the exact sign test;
$\ddagger$ additionally surviving that campaign's Bonferroni correction
($\alpha=0.0063$ on BERT, $0.005$ on the RoBERTa CoLA/STS-B campaign,
$0.00625$ on the RoBERTa RTE and RoBERTa MRPC campaigns and on the three
decoder campaigns). Every delta is paired within a single campaign; the
decoder's B and C on STS-B use the matched campaign's own baseline
(Appendix~\ref{app:decoder}).
\emph{No \textsc{Bert} RTE entry is significant and none could be}: that task has
$277$ evaluation examples and a minimum detectable effect of $2.06$ accuracy
points, larger than any effect in the BERT block. RoBERTa RTE has the same
evaluation set and a similar minimum detectable effect ($1.99$), but two of its
arms produce effects large enough to clear it. So does the decoder's RTE column
($2.04$), where four arms clear it --- but that column's baseline is at chance,
which changes what its gains mean (Appendix~\ref{app:chance}). The decoder's
CoLA column has a minimum detectable effect of $2.96$, the largest of the three
decoder columns, so only the two arms exceeding $10$ MCC resolve there.}
\label{tab:verdict}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
Backbone & Method & CoLA & STS-B & MRPC & RTE \\
\midrule
\multirow{8}{*}{BERT-base}
 & A   & $+0.70$ (11/4) & $+0.03$ (9/5) & $+0.04$ (8/7) & $+0.02$ (6/9) \\
 & B   & $-1.26$ (5/10) & $-1.07^{\ddagger}$ (0/15) & $-0.33$ (5/10) & $+0.99$ (8/7) \\
 & C   & $\mathbf{+1.42}^{\dagger}$ (13/2) & $+0.20$ (12/3) & $-0.32$ (4/11) & $+0.55$ (8/7) \\
 & AB  & $-1.24$ (4/11) & $-0.75^{\ddagger}$ (1/14) & $-0.28$ (5/10) & $+0.79$ (7/6) \\
 & AC  & $+0.45$ (10/5) & $\mathbf{+0.24}$ (11/4) & $-0.15$ (6/8) & $+1.47$ (11/4) \\
 & BC  & $+0.22$ (7/8) & $-1.24^{\ddagger}$ (0/15) & $-0.31$ (5/10) & $\mathbf{+1.85}$ (11/4) \\
 & ABC & $-0.53$ (6/9) & $-0.84^{\ddagger}$ (1/14) & $-0.26$ (4/11) & $+1.06$ (8/6) \\
 & \texttt{no graph} & $+0.83$ (11/4) & $+0.03$ (7/8) & $+0.03$ (9/6) & $+0.07$ (7/6) \\
\midrule
\multirow{10}{*}{RoBERTa-base}
 & A   & $-0.23$ (5/10) & $-0.09$ (5/10) & $-0.05$ (7/8) & $+0.05$ (6/8) \\
 & B   & $-0.37$ (5/10) & $-1.26^{\ddagger}$ (0/15) & $-0.12$ (4/11) & $-1.68^{\dagger}$ (3/12) \\
 & C   & $\mathbf{+0.64}$ (8/7) & $+0.29$ (10/5) & $-0.29^{\dagger}$ (3/12) & $-2.70$ (3/11) \\
 & AB  & $-0.71$ (5/10) & $-0.79^{\ddagger}$ (1/14) & $\mathbf{+0.04}$ (7/8) & $-1.54^{\dagger}$ (3/12) \\
 & AC  & $+0.00$ (6/9) & $+0.06$ (7/8) & $+0.03$ (8/7) & $\mathbf{+0.98}$ (9/6) \\
 & BC  & $-2.16^{\ddagger}$ (0/15) & $-0.26$ (4/11) & $+0.01$ (9/6) & $-1.64$ (4/10) \\
 & ABC & $-3.31^{\ddagger}$ (1/14) & $-0.13$ (8/7) & $-0.06$ (6/9) & $+0.89$ (9/5) \\
 & \texttt{no graph} & $-0.29$ (5/10) & $-0.05$ (7/8) & $-0.03$ (8/7) & $-0.17$ (6/7) \\
 & published $\tau$ & $-0.14$ (8/7) & $\mathbf{+0.32}^{\dagger}$ (12/3) & --- & --- \\
 & quantile $\tau$ & $-1.41^{\dagger}$ (2/13) & $-0.15$ (5/10) & --- & --- \\
\midrule
\multirow{8}{*}{TinyLlama-1.1B}
 & A   & $+1.13$ (9/6) & $+0.04$ (9/6) & --- & $+1.54$ (10/5) \\
 & B   & $\mathbf{-10.26}^{\ddagger}$ (0/15) & $-1.16^{\dagger}$ (2/13) & --- & $+2.99$ (11/4) \\
 & C   & $-2.96^{\dagger}$ (3/12) & $+0.30$ (10/5) & --- & $-0.38$ (8/7) \\
 & AB  & $\mathbf{-13.58}^{\ddagger}$ (0/15) & $-3.56^{\ddagger}$ (0/15) & --- & $+2.00$ (11/4) \\
 & AC  & $+2.43$ (9/6) & $+0.21$ (10/5) & --- & $+4.33^{\dagger}$ (13/2) \\
 & BC  & $-1.06$ (5/10) & $-1.17^{\ddagger}$ (1/14) & --- & $\mathbf{+4.33}^{\ddagger}$ (14/1) \\
 & ABC & $-4.78$ (4/11) & $-4.51^{\ddagger}$ (0/15) & --- & $+2.55$ (11/4) \\
 & \texttt{no graph} & $+2.58$ (10/5) & $+0.00$ (8/7) & --- & $-0.43$ (5/10) \\
\midrule
\multicolumn{6}{l}{\emph{On BERT, every arm containing B is negative on CoLA,
STS-B and MRPC. All four are positive on RTE, none significantly.}} \\
\multicolumn{6}{l}{\emph{Every arm containing C but not B is positive on CoLA
and STS-B, on both encoders.}} \\
\multicolumn{6}{l}{\emph{C is negative on both MRPC columns, and on both the
search gave it a $15\times$ sparser graph than the baseline.}} \\
\multicolumn{6}{l}{\emph{The three STS-B columns agree in sign on all five arms
run on all three backbones.}} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Paired statistics, BERT STS-B: B $\bar{d}=-1.065$, $t=-17.00$, sign
$p=6\times10^{-5}$; BC $-1.242$, $t=-8.30$, $p=6\times10^{-5}$; ABC $-0.837$,
$t=-6.36$, $p=0.00098$; AB $-0.747$, $t=-5.66$, $p=6\times10^{-5}$. All four
survive Bonferroni. BERT CoLA: C $\bar{d}=+1.416$, $t=4.11$, sign $p=0.0074$ ---
significant on both tests but above $\alpha=0.0063$. RoBERTa STS-B: B
$\bar{d}=-1.264$, $t=-15.76$, $p=6\times10^{-5}$; AB $-0.795$, $t=-5.77$,
$p=0.00098$; published $\tau$ $+0.321$, $t=3.13$, $p=0.035$ --- significant on
both tests uncorrected, not after Bonferroni. RoBERTa CoLA: BC $-2.161$,
$t=-6.20$, $p=6\times10^{-5}$; ABC $-3.314$, $t=-5.44$, $p=0.00098$; quantile
$\tau$ $-1.408$, $t=-3.44$, $p=0.0074$. TinyLlama STS-B: B $\bar{d}=-1.163$,
$t=-3.66$, $p=0.0074$ --- significant on both tests, above that campaign's
now-tightened $\alpha=0.00625$; C $+0.291$, $t=1.52$, $p=0.302$; BC $-1.165$,
$t=-4.97$, $p=0.00098$; AB $-3.556$, $t=-6.95$, $p=6\times10^{-5}$; ABC
$-4.506$, $t=-4.85$, $p=6\times10^{-5}$. RoBERTa MRPC: C $\bar{d}=-0.290$,
$t=-3.26$, sign $p=0.0352$ --- significant on both tests uncorrected, not after
Bonferroni; no other arm reaches $0.07$ F1. TinyLlama RTE: BC $\bar{d}=+4.333$,
$t=4.57$, sign $p=0.0010$, the only entry in that column to survive Bonferroni;
AC $+4.332$, $t=4.91$, sign $p=0.0074$; B $+2.985$, $t=2.68$, sign $p=0.0574$;
ABC $+2.553$, $t=2.85$, sign $p=0.119$. TinyLlama CoLA: B $\bar{d}=-10.26$,
$t=-5.14$, sign $p=1\times10^{-4}$; AB $-13.58$, $t=-4.27$, sign
$p=1\times10^{-4}$ --- both survive Bonferroni and are the two largest effects
in any of our fine-tuning tables; C $-2.96$, $t=-2.40$, sign $p=0.0352$,
significant on both tests uncorrected only; ABC $-4.78$, $t=-3.24$, sign
$p=0.119$, which the $t$-test resolves and the sign test does not, so we do not
count it. TinyLlama STS-B, matched campaign: B $\bar{d}=-1.16$, $t=-3.53$, sign
$p=0.0074$, significant on both tests but above $\alpha=0.00625$; C $+0.30$,
$t=1.57$, sign $p=0.302$. Every arm in these tables has exactly
$15$ seeds; shard files and merged campaign files contain the same rows, so the
analysis de-duplicates by run key before testing.

\paragraph{The tally, by direction.} Counting every cell in this paper that
passes \emph{both} the paired $t$-test and the exact sign test, hyperbolic arms
are significantly better than their own baseline in $19$ settings and
significantly worse in $48$; $16$ of the $19$ and $32$ of the $48$ also survive
their family's Bonferroni correction. MTEB supplies most of both counts, at
$\alpha=0.00143$: the BERT block contributes $5$ positives and $11$ negatives
($4$ and $5$ surviving), the RoBERTa block $10$ positives and $19$ negatives
($10$ and $14$ surviving). Outside MTEB the four positives are BC and AC on
decoder RTE (both $+4.33$), C on BERT CoLA ($+1.42$) and A on BERT CoLA at
$n{=}50$ ($+0.83$), of which the first and last survive correction.
\emph{We report this tally as a description of where effects were resolvable,
not as evidence that curvature has a direction}: the counts are dominated by
one benchmark and one stage, and a raw win/loss ratio over non-independent,
differently powered settings is not a meaningful summary statistic. Three
things are worth reading off it. Every one of the three stages appears among
the positives, so no stage is inert. Every one also appears among the negatives:
Stage A, which was clean in the previous revision, is significantly negative on
six of seven RoBERTa retrieval tasks. And the largest effects in either
direction all come from arms containing Stage B, in the same regime. The decoder
RTE entries carry the chance-baseline caveat of Appendix~\ref{app:chance}, and
the RoBERTa MTEB entries the degenerate-baseline caveat of
Appendix~\ref{app:mteb3}.

\subsection{Decomposing Stage C}
\label{app:factorial2x2}

Stage C's selected configuration differed from the baseline's in two ways at
once: a $3.6\times$ denser graph ($q=0.38$ against $0.10$) and a smaller network
($2$ layers of width $128$ against $4$ of $256$). We therefore ran the
$2\times2$ that crosses \emph{message-passing geometry} with
\emph{graph-and-architecture configuration}. The version reported here is the
third and last: all four cells run through a single script at $n=65$,
\emph{after} both control-parity defects of Appendix~\ref{app:defects} were
closed and verified by a forward-pass test. The hyperbolic cells use C's own
tuned curvature settings ($c = 4.0$, HGAT, input clip $0.7$), which is the
choice most favourable to C.

\begin{table}[h]
\caption{Density $\times$ curvature factorial, CoLA MCC, mean $\pm$ sd over
$65$ seeds, with control parity closed. All four cells were run by the
factorial script itself, in one campaign, on one cache state --- no cell is
borrowed from another campaign.}
\label{tab:factorial}
\begin{center}
\small
\begin{tabular}{lcc}
\toprule
 & Euclidean MP & hyperbolic MP \\
\midrule
base config ($q{=}0.10$, $K{=}4$, $h{=}256$) & 44.76\,$\pm$1.90 & 45.26\,$\pm$1.77 \\
C config ($q{=}0.38$, $K{=}2$, $h{=}128$)    & 45.89\,$\pm$1.53 & \textbf{46.62}\,$\pm$1.45 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{Decomposition of Stage C's CoLA gain, paired over $65$ seeds with
control parity closed. The three components sum exactly to the total. The total
is significant on both tests by a wide margin; the curvature term is not
significant on either, and the configuration it was confounded with is more
than twice its size.}
\label{tab:decomp}
\begin{center}
\small
\begin{tabular}{lrrrcc}
\toprule
Component & $\Delta$ & sd & $t$ & sign $p$ & p/n \\
\midrule
geometry alone (at base config)    & $+0.502$ & 2.79 & $1.45$ & $0.136$ & 39/26 \\
configuration alone (Euclidean MP) & $+1.136$ & 2.37 & $3.87$ & $0.0026$ & 45/20 \\
interaction                        & $+0.224$ & 3.57 & $0.51$ & $1.000$ & 33/32 \\
\midrule
\textbf{total} (C config, hyp.\ $-$ base config, Eucl.)
                                   & $\mathbf{+1.862}$ & 2.31 & $\mathbf{6.51}$ & $\mathbf{<10^{-8}}$ & \textbf{51/14} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{Adding Stage C on top of Stage B, across every setting where both
arms were run. Paired differences against each backbone's own baseline, from
Table~\ref{tab:verdict}. The sign of the change is not stable across task or
backbone.}
\label{tab:bxc}
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Setting & B alone & BC & effect of adding C \\
\midrule
BERT STS-B      & $-1.07$ & $-1.24$ & worse \\
RoBERTa STS-B   & $-1.26$ & $-0.26$ & rescues \\
BERT CoLA       & $-1.26$ & $+0.22$ & rescues \\
RoBERTa CoLA    & $-0.37$ & $-2.16$ & much worse \\
RoBERTa MRPC    & $-0.12$ & $+0.01$ & both below the MDE \\
TinyLlama RTE   & $+2.99$ & $+4.33$ & improves an already-positive B \\
TinyLlama STS-B & $-1.16$ & $-1.17$ & no effect \\
TinyLlama CoLA  & $-10.26$ & $-1.06$ & rescues almost entirely \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Each component differences two independently trained cells, so it inherits both
their variances; that is why the parts remain noisier than the whole even at
$n=65$. Three things are worth stating explicitly.

\paragraph{Closing the parity defects did not change the answer.} The same
factorial run before the fixes put the geometry term at $+0.534$; with both
closed it is $+0.502$, a move of $0.032$ MCC. The defects were real and had to
be repaired before any geometry contrast was admissible, but they were not what
made Stage C look good. We report this because the opposite outcome --- a
confound that, once removed, dissolves the effect --- is the one readers expect,
and it is not what happened.

\paragraph{The curvature term is the smaller of the two main effects, and it is
not resolvable.} Geometry is $+0.502$ with $39/26$ seeds ($t=1.45$, sign
$p=0.136$): significant on neither test. Configuration is $+1.136$, significant
on both. So the arm-level advantage Stage C shows on CoLA is real --- the total
is $+1.862$ at $51/14$ --- but it is mostly the denser graph and smaller network
its search happened to select, not the manifold.

\paragraph{The interaction was an artifact of the sample size that suggested
it.} Estimated at $+1.362$ from $15$ seeds, which is why this run was
commissioned, it is $+0.224$ at $65$ with $33/32$ seeds positive. The same
arithmetic that made $65$ seeds the right budget at $\Delta=1.36$ makes them
nowhere near enough at $\Delta=0.22$: detecting a term that small against
$\mathrm{sd}=3.57$ at $80\%$ power would take roughly $2{,}000$ seeds, about
$75$ GPU-days for this one contrast. We do not propose to run it, and we do not
claim the interaction is zero --- only that it is not resolvable at any budget
this project can reach.

\section{Porting to other backbones}
\label{app:calib}

\begin{table}[h]
\caption{Token-cosine percentiles and the edge density the published grid
$\tau \in \{0.1,0.3,0.6\}$ actually yields. Measured on $192$ CoLA validation
sentences at the final layer, \texttt{max\_length} $128$, off-diagonal pairs
only, self-loops excluded. This is a property of the backbone and $\tau$ alone.
Density near $1$ means the GNN sees a nearly complete graph.}
\label{tab:density}
\begin{center}
\small
\begin{tabular}{lcccccc}
\toprule
Backbone & $\cos$ p10 & $\cos$ p50 & $\cos$ p90 & $\tau{=}0.1$ & $\tau{=}0.3$ & $\tau{=}0.6$ \\
\midrule
TinyLlama-1.1B   & 0.016 & 0.343 & 0.588 & 0.762 & 0.533 & \textbf{0.073} \\
ModernBERT-base  & 0.076 & 0.648 & 0.768 & 0.866 & 0.759 & 0.599 \\
BERT-base        & 0.098 & 0.328 & 0.595 & 0.876 & 0.505 & \textbf{0.103} \\
SmolLM2-360M     & 0.322 & 0.774 & 0.911 & 0.956 & 0.908 & 0.741 \\
RoBERTa-base     & \textbf{0.693} & 0.821 & 0.897 & 1.000 & 1.000 & \textbf{0.991} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The failure is layer-dependent: ModernBERT reaches $0.599$ at the final layer
but $0.996$ at layer $12$. \citet{mantri2026glot} do not state which layer they
pool, so ``which backbones are affected'' cannot be settled from the published
description; we treat RoBERTa as the one clear case. This table is also why we
chose TinyLlama over SmolLM2 as the decoder: its density at the published
$\tau=0.6$ ($0.073$) is close to BERT's ($0.103$), whereas SmolLM2 sits at
$0.741$.

\begin{table}[h]
\caption{RoBERTa-base calibration arms, $n=15$ paired seeds, tuned per arm and
per task on RoBERTa itself ($40$ trials each). \texttt{published\_tau} pins the
published grid and gives the near-complete graph above; \texttt{quantile\_tau}
is the density-matching repair. The repair is the worst of these four on both
tasks --- despite having \emph{won} the tuning stage ($56.77$, best of five).
The hyperbolic arms tuned on the same backbone are in
Table~\ref{tab:verdict}.}
\label{tab:roberta}
\begin{center}
\small
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{3}{c}{CoLA (MCC)} & \multicolumn{3}{c}{STS-B (Spearman)} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Arm & mean & $\Delta$ & p/n & mean & $\Delta$ & p/n \\
\midrule
baseline                & \textbf{54.60} & \emph{ref} & --- & 83.89 & \emph{ref} & --- \\
\texttt{published\_tau} & 54.46 & $-0.14$ & \phantom{0}8/7 & \textbf{84.21} & $\mathbf{+0.32}$ & 12/3 \\
A                       & 54.37 & $-0.23$ & \phantom{0}5/10 & 83.80 & $-0.09$ & \phantom{0}5/10 \\
\texttt{quantile\_tau}  & 53.19 & $\mathbf{-1.41}$ & \phantom{0}2/13 & 83.73 & $-0.15$ & \phantom{0}5/10 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Forcing the graph sparse costs $1.41$ MCC ($t=-3.44$, $2/13$, sign $p=0.0074$).
An earlier revision reported this as surviving Bonferroni at $\alpha=0.0125$,
which was correct when only the four calibration arms were tested against this
baseline. The RoBERTa campaign now puts ten arms against the same baseline, so
the correction tightens to $\alpha=0.005$ and the repair's deficit no longer
survives it. The effect is unchanged; only the number of comparisons it must be
judged against. Against \texttt{published\_tau} directly the repair loses on both
tasks ($-1.27$ CoLA, $-0.48$ STS-B, the latter $2/13$, $p=0.0074$).

\paragraph{Detail behind \S\ref{sec:porting}.} The quantile repair costs $1.41$
MCC ($t=-3.44$, $2/13$, sign $p=0.0074$) and also \emph{won} its tuning stage
before finishing last over $15$ seeds, so selection actively favoured it. The
\texttt{no graph} control costs $0.29$ MCC on RoBERTa CoLA ($5/10$) and $0.05$
Spearman on RoBERTa STS-B ($7/8$), neither close to significant; on BERT it is
$+0.83$ MCC on CoLA ($11/4$, $p=0.118$, second of nine arms), $+0.03$ Spearman,
$+0.03$ F1 and $+0.07$ accuracy. Against \texttt{no graph} rather than the
baseline, Stage C is $+0.16$ ($12/3$) on BERT STS-B and $+0.58$ ($8/7$,
$p=1.00$) on BERT CoLA, despite the $+1.42$ it shows against the baseline.
ModernBERT \citep{warner2024modernbert} and SmolLM2 \citep{benallal2025smollm2}
appear in calibration roles only. Cost is why the four tasks are the four
smallest: $495$ runs per task is $22$ hours for CoLA, against an estimated $108$
for SST-2 and $185$ and $124$ for QQP and MNLI, with $75$\,GB of cached states
between the latter two.

\paragraph{Density and scale are separate.} We had credited a $21.71 \rightarrow
27.15$ MCC recovery on ModernBERT/CoLA to density-matching plus median-norm
rescaling, measured at one seed with both factors moved together. The
$2\times3$ factorial over $5$ seeds (Table~\ref{tab:fix}) separates them. The
choice of rescaling statistic matters far more than whether you rescale, and it
is the only effect here significant on both tests: on ModernBERT layer $12$,
whose mean/median norm ratio is $7.66$, mean-based (\texttt{rms}) rescaling
costs $11.03$ MCC ($t=-5.47$, $0/5$) --- the mean is exactly the statistic
outliers corrupt. Median rescaling is directionally positive ($+5.96$ and
$+5.32$ on layer $12$, $+1.34$ at the final layer with $5/0$) but reaches
significance nowhere at $n=5$. Density-matching has no consistent sign
($+4.58$, $-1.91$, $-0.25$, $+0.47$). BERT is inert throughout, every contrast
at most $0.27$ MCC --- our earlier single-seed reading of that control
($45.54 \rightarrow 43.41$) was noise.

\begin{table}[h]
\caption{Density $\times$ scale factorial, CoLA MCC, mean $\pm$ sd over $5$
seeds. Columns vary the threshold, rows vary token-norm rescaling. At $n=5$ the
exact sign test cannot fall below $p=0.0625$, so nothing here can meet our
both-tests bar; we report it as a lead.}
\label{tab:fix}
\begin{center}
\small
\begin{tabular}{llcc}
\toprule
Backbone (layer) & Rescaling & $\tau=0.6$ (pub.) & $q=0.05$ \\
\midrule
\multirow{3}{*}{BERT-base (final) \emph{control}}
  & none            & 45.25\,$\pm$1.09 & 45.00\,$\pm$1.69 \\
  & \texttt{rms}    & 45.01\,$\pm$1.39 & 45.03\,$\pm$1.32 \\
  & \texttt{median} & 44.98\,$\pm$1.12 & 45.08\,$\pm$0.93 \\
\midrule
\multirow{3}{*}{ModernBERT-base (L12)}
  & none            & 18.64\,$\pm$11.36 & 23.22\,$\pm$6.90 \\
  & \texttt{rms}    & \phantom{0}7.61\,$\pm$8.62 & 19.18\,$\pm$3.33 \\
  & \texttt{median} & 24.60\,$\pm$7.96 & \textbf{28.54}\,$\pm$2.69 \\
\midrule
\multirow{3}{*}{ModernBERT-base (final)}
  & none            & 37.53\,$\pm$1.21 & 35.62\,$\pm$6.70 \\
  & \texttt{rms}    & 38.64\,$\pm$1.66 & 37.61\,$\pm$1.92 \\
  & \texttt{median} & \textbf{38.87}\,$\pm$1.32 & 37.57\,$\pm$2.00 \\
\midrule
\multirow{3}{*}{RoBERTa-base (final)}
  & none            & 52.39\,$\pm$2.33 & \textbf{52.87}\,$\pm$0.96 \\
  & \texttt{rms}    & 52.55\,$\pm$1.55 & 52.63\,$\pm$1.42 \\
  & \texttt{median} & 52.54\,$\pm$1.41 & 52.72\,$\pm$1.40 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\section{Decoder backbone}
\label{app:decoder}

TinyLlama-1.1B on STS-B, $10$ tuning trials per arm and $n=15$ shared
confirmation seeds. The trial budget is lower than the BERT campaigns' $40$
because a run here costs about four times as much; it is identical across the
arms in this campaign, which is what the paired tests require, but it does mean
these arms were selected from a coarser search than their BERT counterparts.
Backbone chosen over SmolLM2 on measured graph density
(Table~\ref{tab:density}). Minimum detectable effect $0.503$ on STS-B, $0.556$
in the matched campaign below and $2.961$ on CoLA. The absolute
scores are in Table~\ref{tab:absolute} and the deltas in
Table~\ref{tab:verdict}; the ordering matches BERT, and the standard deviation
grows in step with the number of hyperbolic parts
($0.49 \rightarrow 0.95 \rightarrow 1.87 \rightarrow 3.58$).

\paragraph{B and C alone were rerun in a matched campaign, and it reproduced
the splice.} Both arms were dropped from the original sweep for cost. They were
first run into their own results file four days later --- a file containing no
baseline, so any delta for them subtracted a cell from a different campaign,
the practice Appendix~\ref{app:determinism} shows is unsafe. Rather than test
that difference we reran the contrast properly: baseline, B and C, $15$ shared
seeds, one campaign, one cache state.

The matched campaign gives baseline $79.94 \pm 0.52$, B $78.78 \pm 1.03$ and
C $80.24 \pm 0.52$. All three reproduce the spliced values to two decimals
(the original campaign's baseline was $79.95 \pm 0.49$). \emph{The splice was
accurate, and refusing to rely on it still cost us nothing.} This is what
Appendix~\ref{app:determinism} predicts: pairing halves the variance on STS-B
and is worse than useless on CoLA, so STS-B is exactly where cross-campaign
differencing survives. It does not generalise --- the CoLA factorial moved the
same contrast from $+0.40$ to $+0.07$ to $+0.53$ depending on how the shared
cell was obtained --- and we would make the same call again.

B alone is therefore negative on the decoder with an admissible test:
$\bar{d}=-1.16$, $t=-3.53$, sign $p=0.0074$, $2/13$ seeds. That is significant
on both tests but above the $\alpha=0.00625$ we quote for this backbone. C alone
is $+0.30$ ($10/5$) and not significant on either test.

\paragraph{The decoder's CoLA column.} Nine arms, same $10$ trials and $15$
confirmation seeds, minimum detectable effect $2.96$ --- the least sensitive
of the three decoder columns, so only large effects resolve there. Two do, and
they are the largest in any of our fine-tuning tables: B costs $10.26$ MCC and
AB costs $13.58$, both $0/15$ and both Bonferroni-significant. Two of AB's
fifteen seeds return $\mathrm{MCC}=0$ exactly --- the classifier predicts the
majority class --- so part of that $13.58$ is outright training failure rather
than degraded quality; excluding those two seeds AB is about $-9$. We keep them
in the reported mean, because an arm that sometimes fails to train has told us
something about itself. Stage C alone costs $2.96$ MCC here, significant on both
tests uncorrected, which is the opposite sign to its BERT CoLA result.

On the Bonferroni count, the strict reading of our own rule --- the correction is
a property of a campaign --- leaves the original decoder campaign at six arms
and $\alpha=0.0083$, with the matched baseline/B/C campaign forming a separate
two-comparison family. We nevertheless quote the tighter $\alpha=0.00625$ that
treats all eight as one family, because it is the conservative choice and
because the arms do share a baseline in practice. Under either count the three
arms that previously carried $\ddagger$ still survive, and B alone does not.

The decoder MRPC column is \emph{deliberately} not run. MRPC resolves nothing on
either encoder --- its whole nine-arm spread is $0.37$ F1 on BERT and $0.33$ on
RoBERTa against a minimum detectable effect of $0.317$ --- so a third such column
would buy a third null for about $22$ GPU-hours. Those cells are dashes in
Tables~\ref{tab:absolute} and~\ref{tab:verdict}.

\section{The decoder's RTE baseline is at chance}
\label{app:chance}

The decoder RTE column contains the only B-containing arm in our fine-tuning
tables that is significantly \emph{better} than its baseline, so it deserves a
check the other columns do not need: is the baseline actually learning the task?

RTE's development set has $277$ examples and a majority-class rate of
$52.708\%$ ($146/277$). A model that always predicted the majority label would
score exactly that. Two reference quantities follow. A model guessing
independently per example has a seed-to-seed standard deviation of
$\sqrt{p(1-p)/n} = 3.004$ accuracy points; a model that has learned something
stable should sit well below that.

\begin{table}[h]
\caption{Decoder RTE against the majority-class rate rather than against the
baseline. ``sd/coin'' is the arm's seed standard deviation divided by the
$3.004$ points a coin-flipping model would show; $t$ and $p$ are a one-sample
test of the arm's $15$ seeds against $52.708$. The baseline, \texttt{no graph}
and C are statistically indistinguishable from a majority-class predictor and
scatter as widely as guessing would.}
\label{tab:chance}
\begin{center}
\small
\begin{tabular}{lccccc}
\toprule
Arm & mean & sd & sd/coin & $t$ vs.\ majority & $p$ \\
\midrule
\textsc{Glot} (baseline) & 52.88 & 2.92 & 0.97 & $0.22$ & $0.83$ \\
\texttt{no graph}        & 52.44 & 2.89 & 0.96 & $-0.35$ & $0.73$ \\
C                        & 52.49 & 2.56 & 0.85 & $-0.33$ & $0.75$ \\
\midrule
A                        & 54.42 & 2.29 & 0.76 & $2.89$ & $0.012$ \\
AB                       & 54.87 & 3.00 & 1.00 & $2.79$ & $0.014$ \\
ABC                      & 55.43 & 2.96 & 0.99 & $3.56$ & $0.0032$ \\
B                        & 55.86 & 2.88 & 0.96 & $4.24$ & $0.00082$ \\
BC                       & \textbf{57.21} & 1.70 & 0.57 & $10.24$ & $7.0{\times}10^{-8}$ \\
AC                       & \textbf{57.21} & 1.57 & \textbf{0.52} & $11.13$ & $2.5{\times}10^{-8}$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Three consequences, and they do not all point the same way.

\paragraph{The $+4.33$ is real, and it is not a small-margin artifact.} AC and
BC do not merely beat the baseline; they beat the majority-class rate by
$4.5$ points at $p \approx 10^{-8}$, and their seed scatter is roughly half
what guessing would produce. Something is being learned. On this column the
Euclidean baseline is the arm that fails.

\paragraph{But the comparison is not the one the rest of the paper makes.}
Everywhere else in this paper the baseline is a working model and the question
is whether curvature improves it. Here the baseline sits at chance
($p=0.83$ against the majority rate), so $+4.33$ measures the distance from
\emph{not learning} to \emph{learning}, not an improvement to a competent
pooler. Those are different quantities and we do not pool them. This is why the
decoder RTE entries are excluded from the effect-size comparisons of
\S\ref{sec:stageB} and reported separately in the tally of
Appendix~\ref{app:results}.

\paragraph{And it cuts against Stage C, not for it.} C alone is $52.49$ ---
also at chance, and nominally below the baseline. The arms that clear chance
are the ones containing A or B. So the one column where a B-containing arm wins
outright is also a column where Stage C on its own does nothing, which is the
opposite of the GLUE pattern. We report it and offer no mechanism for it.

\section{Noise diagnostic}
\label{app:stress}

We reproduce the signal-in-noise test of \citet{mantri2026glot} (their Table 7):
a short signal phrase carrying a logical dependency, injected into random
distractor words, with the distractor ratio swept from $20\%$ to $90\%$. BERT,
mean $\pm$ sd over $5$ seeds; the original reports one seed. We follow their
layout: methods as rows, distractor ratios as columns, best per column in bold.

\begin{table}[h]
\caption{Diagnostic stress test, BERT-base, accuracy $\times100$, mean $\pm$ sd
over $5$ seeds. The upper block is our hyperbolic arms, the middle block the
controls, the lower block the poolers of the original. Stage A appears twice:
the Poincar\'e graph can be built by thresholding distance at a quantile or by
$k$-NN, and the stress harness supports both.}
\label{tab:stress}
\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
Method & 20\% & 50\% & 80\% & 90\% \\
\midrule
A ($k$-NN)          & 97.0\,$\pm$1.3 & 91.9\,$\pm$4.1 & 91.0\,$\pm$2.7 & 84.9\,$\pm$9.1 \\
A (quantile)        & 96.5\,$\pm$1.0 & 91.5\,$\pm$6.5 & 89.8\,$\pm$5.7 & 83.1\,$\pm$8.2 \\
B                   & 95.4\,$\pm$1.4 & 88.7\,$\pm$2.8 & 83.8\,$\pm$13.8 & 75.5\,$\pm$14.9 \\
C                   & 95.0\,$\pm$3.3 & 87.6\,$\pm$11.2 & 90.8\,$\pm$5.9 & 88.4\,$\pm$8.8 \\
AC                  & 95.0\,$\pm$3.4 & 88.2\,$\pm$11.2 & 90.8\,$\pm$5.8 & \textbf{88.5}\,$\pm$8.9 \\
ABC                 & 88.7\,$\pm$12.3 & 82.5\,$\pm$15.4 & 86.0\,$\pm$10.5 & 84.5\,$\pm$8.9 \\
\midrule
\textsc{Glot} (baseline) & 96.0\,$\pm$0.5 & 90.2\,$\pm$5.0 & 88.1\,$\pm$4.7 & 79.8\,$\pm$10.8 \\
\texttt{no graph}   & \textbf{98.0}\,$\pm$1.3 & \textbf{94.8}\,$\pm$5.7 & \textbf{92.5}\,$\pm$5.0 & 84.4\,$\pm$10.1 \\
\textsc{Glot} $K{=}0$ & 95.7\,$\pm$1.6 & 91.0\,$\pm$3.6 & 89.3\,$\pm$5.9 & 88.1\,$\pm$6.5 \\
\midrule
AdaPool             & 95.1\,$\pm$1.2 & 90.8\,$\pm$2.7 & 86.5\,$\pm$6.4 & 83.6\,$\pm$8.4 \\
Mean                & 73.5\,$\pm$3.2 & 63.2\,$\pm$5.2 & 64.0\,$\pm$1.6 & 57.4\,$\pm$3.2 \\
\texttt{[CLS]}      & 70.0\,$\pm$1.7 & 61.4\,$\pm$2.9 & 62.1\,$\pm$3.2 & 65.4\,$\pm$2.4 \\
Max                 & 63.0\,$\pm$3.4 & 55.2\,$\pm$2.8 & 53.1\,$\pm$1.8 & 51.8\,$\pm$2.6 \\
\midrule
\multicolumn{5}{l}{\emph{\citet{mantri2026glot} Table 7, BERT rows (single seed):}} \\
\textsc{Glot}       & 97.2 & 97.0 & 97.8 & 98.8 \\
AdaPool             & 91.4 & 78.8 & 65.6 & 61.6 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Three readings, in decreasing order of confidence.

\paragraph{\textsc{Glot}'s advantage over bag-of-tokens pooling is real and
holds in every seed.} Mean, \texttt{[CLS]} and Max lose $14$--$35$ points at
every ratio, $0/5$ seeds positive. This is the claim the original rests on and
we reproduce it.

\paragraph{But the graph is not what produces it.} \texttt{no graph} --- the
same trained pooler with every edge deleted --- is the best arm at three of the
four ratios, and \textsc{Glot} with $K{=}0$ GNN layers beats \textsc{Glot} at
$80\%$ and $90\%$. Whatever survives the noise here is the trained projection
and readout, not message passing. Stage A is ahead of the baseline at all four
ratios but behind \texttt{no graph} at three; Stage B is last of the hyperbolic
arms, consistent with \S\ref{sec:stageB}. An earlier revision of ours reported
``Stage A wins the stress test''; with the control in the table, the honest
statement is that removing the graph wins.

\paragraph{We do not reproduce the published trained-pooler numbers.} At $90\%$
distractors the original reports $98.8$ where we measure $79.8 \pm 10.8$ for the
same method, and their curve is flat or rising under noise where ours falls. The
divergence is not uniform: the static poolers agree closely ($-2.2$ to $+5.6$
across twelve cells), while both \emph{trained} poolers diverge in opposite
directions, so the $37$-point margin their Table 7 reports for \textsc{Glot}
over AdaPool at $90\%$ becomes $-3.9$ in our hands. Their Appendix~B.4 does not
give the vocabulary source, the signal templates, or the pooler budget, so we
cannot localise the cause. We flag this as unresolved, and we never use the
published values as a baseline.

\section{GNN choice and training recipe}
\label{app:factorial}

\paragraph{GNN backbone.} Over $5$ seeds at the published recipe, CoLA gives GAT
$45.38 \pm 1.19$, GCN $45.14 \pm 1.78$, GIN $44.86 \pm 1.33$; STS-B gives
$82.69 \pm 0.34$, $82.46 \pm 0.27$, $82.47 \pm 0.32$. Spread $0.52$ MCC and
$0.23$ Spearman, inside seed noise, and GAT is the only one of the three that
consumes edge attributes at all.

\paragraph{The STS-B residual is not a recipe mismatch.} The released README
trains differently from the paper's Appendix~B.2 ($3$ epochs,
\texttt{scorer\_hidden} $256$, $\tau = 0.8$ against $2$, $128$, $0.6$). We ran
the $2\times2\times2$ decomposition at seed $42$. Epoch count is the only factor
that moves anything ($82.36 \rightarrow 82.66$); $\tau$ and
\texttt{scorer\_hidden} are inert to within $0.04$. Over $5$ seeds the two
recipes are indistinguishable ($82.69 \pm 0.34$ against $82.69 \pm 0.23$). The
best cell, $82.66$, is still $1.20$ below the published $83.86$, so the residual
stands.

\paragraph{Layer choice overfits.} Layer $8$ beat layer $12$ by $+4.62$ MCC on
CoLA but only $+0.25$ on STS-B, and lost on RTE ($-0.36$) and MRPC ($-0.46$).

\paragraph{A structural prior does not help either.} Since CoLA, MRPC and RTE
are flat for every geometric arm, we also asked whether a \emph{structural}
prior helps where a similarity prior does not: positional edges joining tokens
with $|i-j| \leq w$ (\texttt{POS}), positional edges only
(\texttt{POS\_ONLY}), their union with Stage A (\texttt{A\_POS}), and a $k$-NN
graph (\texttt{KNN}). All four were run on BERT CoLA at the same $15$
confirmation seeds as the rest of the paper: \texttt{POS} $46.10 \pm 1.52$,
\texttt{A\_POS} $46.06 \pm 1.63$, \texttt{KNN} $45.75 \pm 1.44$, and
\texttt{POS\_ONLY} $46.61 \pm 0.85$ at $n=4$ only.

\emph{These are absolute scores, not differences.} That campaign tuned a
baseline but never ran one at the confirmation seeds, so differencing against
the wide campaign's $45.37$ would be the cross-campaign splice of
Appendix~\ref{app:determinism}, and we do not do it. Taken at face value the
four sit within about $0.7$ MCC of each other and of every geometric arm on
that task, which is the same flatness the hyperbolic arms show; we record the
experiment so that ``did you try a non-geometric graph?'' has an answer, and
draw no conclusion from it.

\section{A retracted result of our own}
\label{app:narrow}

Under the graph-only search, Stage A on STS-B gave $\bar{d}=+0.223$,
$t(14)=6.20$, sign $p=6\times10^{-5}$, $15/15$ seeds positive, and replicated on
$12$ held-out seeds. It does not survive the wide search: $\bar{d}=+0.028$,
$t=0.69$, $9/5$. The effect was real but its cause was not geometry --- the
baseline had been pinned at $K=2$, $h=128$ while nothing else was, and it
recovers the whole gap once it can choose $K=4$. This is the clearest
illustration of the paper's own point: a pooling comparison is only as good as
the budget given to its control.

\section{Long-text and retrieval benchmarks}
\label{app:docmteb}

\citet{mantri2026glot} also report IMDB (long-text classification, their
Table 2) and MTEB \citep{muennighoff2022mteb}. We ran both, early in the
project, at a single seed and on BERT only. We report them here for coverage and
draw no statistical conclusion: with $n=1$ there is no test to run.

\paragraph{Two caveats that make this section evidence of nothing.} First, the
MTEB rows evaluate an \emph{untrained} pooler: the driver never set
\texttt{checkpoint\_path}, and the evaluation entry point loads pooler weights
only when it is set, so every row below is a randomly initialised pooler on a
frozen backbone. The wall-clock times confirm it ($7$--$171$ seconds per task,
which is inference and nothing else), and the three columns agree to the third
decimal on five of seven tasks. Second, the seven tasks are ones we selected
from the original's \emph{appendix} Table 12, not the seven of its Table 3;
only SciFact and STS12 are common to both. We leave the table in place because
an earlier draft described these rows as trained and as matching the original's
main table, and silently deleting it would hide two corrections. The IMDB rows
are unaffected: that task trains the pooler normally.

\paragraph{One of these rows is not what its name says.} These runs predate our
switch to quantile thresholding. The $\rho$-defect arm used an absolute radius
$\rho = 1.0$ on unnormalised BERT features, and as Appendix~\ref{app:defects}
explains, real Poincar\'e distances there lie in $[8.85, 11.76]$ --- so that arm
trained on an empty graph and is \emph{not} a Stage A result. We keep the column
only so it cannot be mistaken for one. The $k$-NN column is a genuine Stage A
variant.

\begin{table}[h]
\caption{IMDB accuracy and MTEB main scores ($\times 100$), BERT-base, seed
$42$, one run per cell. The middle column is the $\rho$-defect run described
above; it trained on an empty graph and is not a Stage A measurement.
\textbf{The MTEB block evaluates an \emph{untrained} pooler} --- we did not run
the MS MARCO contrastive stage --- so those three columns differ only by random
initialisation and measure nothing about the method.}
\label{tab:docmteb}
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
 & \textsc{Glot} & $\rho$-defect$^{\dagger}$ & Stage A \\
 & (cosine) & (empty graph) & ($k$-NN) \\
\midrule
IMDB (acc.) & 92.26 & \textbf{92.50} & 92.46 \\
\midrule
Banking77Classification    & 46.91 & 46.90 & \textbf{46.91} \\
STS12                      & 30.02 & \textbf{30.04} & 27.68 \\
STS13                      & 59.57 & 59.60 & \textbf{59.83} \\
SciFact                    & 13.93 & \textbf{14.10} & 13.39 \\
ArguAna                    & \textbf{28.25} & 28.11 & 27.73 \\
TwentyNewsgroupsClustering & 24.49 & 24.13 & \textbf{25.10} \\
SprintDuplicateQuestions   & \textbf{34.67} & 34.57 & 34.40 \\
\midrule
MTEB mean                  & \textbf{33.98} & 33.92 & 33.58 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Nothing here is separable at one seed, and neither Stage B nor Stage C was ever
run on these benchmarks. Doing IMDB properly is expensive --- our runs took
about $3.8$ hours each at $512$ tokens, so eight arms $\times$ $15$ seeds would
be roughly $20$ days --- whereas MTEB is cheap to \emph{evaluate} (all seven
tasks take about eight minutes) once the MS MARCO contrastive stage exists.

\subsection{MTEB with a pooler that was actually trained}
\label{app:mteb3}

The corrected pipeline was run as a full campaign: six arms $\times$ $15$ shared
seeds, on the original's \emph{Table 3} task list, every task name resolved
against the installed \texttt{mteb} registry rather than guessed. Both encoder
blocks are complete --- BERT and RoBERTa, six arms at $15$ seeds, seven tasks
each. The RoBERTa block cost us a day first: the queue passed
\texttt{roberta-base} while the driver's model registry is keyed on
\texttt{FacebookAI/roberta-base}, so all $90$ of its runs were skipped in under
a second with a warning and the queue moved on. No compute was spent and nothing
failed loudly enough to stop the queue --- a name mismatch that a row count, not
a log, was needed to catch. It belongs with the defects of
Appendix~\ref{app:defects}, except that this one is ours.

The contrastive stage is worth a great deal on its own: on the two tasks common
to both task lists, the baseline moves $30.02 \rightarrow 41.21$ on STS12 and
$13.93 \rightarrow 19.72$ on SciFact. Table~\ref{tab:docmteb} was measuring
random initialisation, as its caption says.

\paragraph{We do not reproduce the published absolute numbers, and one cell is
broken.} Our baseline sits below \citet{mantri2026glot}'s Table 3 on twelve of
the fourteen encoder cells, and in every one of those twelve the published value
is above our \emph{best} of $15$ seeds --- so the gap is systematic, not a draw
from our seed distribution. It is $0.85$--$4.69$ points on BERT and
$0.22$--$5.59$ on RoBERTa, except for RoBERTa SciFact, where we score $0.58$
ndcg@$10$ against a published $26.05$ and no seed exceeds $1.03$. That last one
is a defect rather than a shortfall --- BERT's SciFact reproduces to within the
ordinary gap --- and we have not found its cause. The most likely explanation for
the systematic part is the MS MARCO contrastive stage, the one component MTEB
depends on and GLUE does not; our GLUE reproduction is sound
(Appendix~\ref{app:repro}). \emph{No comparison in this appendix depends on the
absolute level}: every delta is paired against our own baseline on the same
seeds. But the two SciFact columns should be read as effects on a broken
baseline, and we exclude them from the effect-size comparisons of
\S\ref{sec:stageB}.

\begin{table}[h]
\caption{MTEB, the original's Table 3 task list, BERT-base, $15$ shared seeds,
$\times100$. The pooler is contrastively trained on MS MARCO before each
evaluation. The first column is the absolute baseline mean; the rest are paired
differences against it on the same seeds. $\dagger$ significant on both tests
uncorrected, $\ddagger$ also after
Bonferroni over the whole arm-by-task family for this backbone
($5$ arms $\times$ $7$ tasks $=35$ contrasts, $\alpha=0.00143$). The seven
metrics are not
commensurable --- accuracy, ndcg@$10$, v-measure, map@$1000$, cosine-Spearman,
max-AP --- so \emph{no row average is given}, and for the same reason we drop
the ``MTEB mean'' row that Table~\ref{tab:docmteb} carried.}
\label{tab:mteb3}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccccc}
\toprule
Task (metric) & baseline & A & B & C & AC & ABC \\
\midrule
EmotionClassification (acc.)   & 34.90 & $+0.27^{\dagger}$ & $-3.04^{\ddagger}$ & $+0.26$ & $+0.39$ & $-3.36^{\ddagger}$ \\
SciFact (ndcg@10)              & 19.72 & $+0.04$ & $+3.86^{\ddagger}$ & $-1.95^{\dagger}$ & $-1.87^{\dagger}$ & $\mathbf{+5.57}^{\ddagger}$ \\
RedditClustering (v-meas.)     & 35.92 & $+0.21$ & $-0.23$ & $-0.64$ & $-0.55$ & $-0.53$ \\
AskUbuntuDupQuestions (map)    & 48.06 & $+0.11$ & $-0.51^{\dagger}$ & $-0.18$ & $-0.07$ & $-0.76^{\ddagger}$ \\
STS12 (Spearman)               & 41.21 & $+0.43$ & $\mathbf{+10.46}^{\ddagger}$ & $-1.79$ & $-1.68$ & $+10.30^{\ddagger}$ \\
TwitterSemEval2015 (max-AP)    & 52.81 & $+0.10$ & $\mathbf{-15.54}^{\ddagger}$ & $-0.64$ & $-0.67^{\dagger}$ & $-15.34^{\ddagger}$ \\
SummEval (Spearman)            & 29.44 & $-0.05$ & $-1.22^{\dagger}$ & $+0.30$ & $+0.23$ & $-1.38^{\dagger}$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{MTEB, the original's Table 3 task list, RoBERTa-base, $15$ shared
seeds, $\times100$, same protocol and same family-wise correction as
Table~\ref{tab:mteb3}. The baseline column is not a faithful reproduction of
the published numbers and SciFact in particular is degenerate; see the text.}
\label{tab:mteb3rob}
\begin{center}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccccc}
\toprule
Task (metric) & baseline & A & B & C & AC & ABC \\
\midrule
EmotionClassification (acc.)   & 23.38 & $-2.40^{\ddagger}$ & $-0.51$ & $-1.85^{\ddagger}$ & $-2.35^{\ddagger}$ & $+5.89^{\ddagger}$ \\
SciFact (ndcg@10)              & \phantom{0}0.58 & $-0.13$ & $+5.08^{\ddagger}$ & $-0.27^{\dagger}$ & $+0.34$ & $+7.32^{\ddagger}$ \\
RedditClustering (v-meas.)     & 16.97 & $-2.89^{\ddagger}$ & $+4.29^{\ddagger}$ & $-2.52^{\ddagger}$ & $-2.88^{\ddagger}$ & $\mathbf{+10.35}^{\ddagger}$ \\
AskUbuntuDupQuestions (map)    & 45.25 & $-0.50^{\dagger}$ & $+0.36$ & $-0.41^{\dagger}$ & $-1.14^{\ddagger}$ & $+2.06^{\ddagger}$ \\
STS12 (Spearman)               & 28.97 & $-3.46^{\dagger}$ & $+12.80^{\ddagger}$ & $-4.72^{\dagger}$ & $-6.95^{\ddagger}$ & $\mathbf{+17.05}^{\ddagger}$ \\
TwitterSemEval2015 (max-AP)    & 54.28 & $-3.58^{\ddagger}$ & $-6.13^{\ddagger}$ & $-2.38^{\ddagger}$ & $-5.77^{\ddagger}$ & $\mathbf{-10.79}^{\ddagger}$ \\
SummEval (Spearman)            & 23.44 & $-3.27^{\ddagger}$ & $+4.68^{\ddagger}$ & $-0.71$ & $+0.13$ & $+2.13^{\ddagger}$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\paragraph{This benchmark can resolve the effects at issue.} Baseline seed
standard deviations are $0.28$--$1.50$, giving minimum detectable effects of
$0.21$--$1.12$; five of the seven tasks are better powered than BERT CoLA
($1.153$) and all seven are far better powered than RTE ($2.064$). Unlike
Table~\ref{tab:docmteb}, nothing here is excused by sample size.

\paragraph{Stage B reverses direction between the two encoders.} On BERT it is
positive on two tasks and negative on four. On RoBERTa it is positive on four
($+12.80$ on STS12, $+5.08$ on SciFact, $+4.68$ on SummEval, $+4.29$ on
RedditClustering, all $15/15$) and negative on one ($-6.13$, $0/15$). The
largest negative in the paper, $-15.54$ on BERT TwitterSemEval2015, does not
replicate in magnitude: the same contrast on RoBERTa is $-6.13$. The largest
positive does replicate and grows.

\paragraph{Stage A loses its clean record here.} Everywhere else in this paper
Stage A is small and mostly positive. On RoBERTa retrieval it is negative on six
of seven tasks, four of them at $0/15$ seeds surviving Bonferroni. Stage C is
negative on six of seven and AC on five of seven. On this backbone the graph and
the message passing both hurt, and only the readout helps.

\paragraph{ABC does not track B on RoBERTa, and this is the sharpest
interaction we measured.} On BERT, ABC reproduces B's pattern on all seven tasks
and on the same side of zero, so the readout sets the outcome and the graph and
message passing ride along. On RoBERTa the same three stages compose
differently: A alone, C alone and AC are all significantly negative, yet ABC is
positive on six of seven tasks and \emph{larger than B alone on every one of
them} --- $+17.05$ against $+12.80$ on STS12, $+10.35$ against $+4.29$ on
RedditClustering, $+5.89$ against $-0.51$ on EmotionClassification. Stages that
hurt in isolation help when stacked on the hyperbolic readout. We have no
account of why. It is the same lesson as \S\ref{sec:interaction} in its
strongest form: how the stages combine is a property of the regime, not of the
operators.

\paragraph{What the deltas do not say.} The RoBERTa gains are large partly
because the RoBERTa baseline is poor. ABC's $+17.05$ takes STS12 from $28.97$ to
$46.15$, which is still below our own BERT ABC ($51.51$) and below the published
BERT \textsc{Glot} ($48.62$). The pattern is consistent with, though not proof
of, the density story of Appendix~\ref{app:calib}: RoBERTa's cosine graph is
$0.991$ dense, the Euclidean attention readout over a near-complete graph is
weak, and the gyro-midpoint recovers part of what that costs --- while Stage A,
which repairs density directly, makes it worse. We report the correlation and
did not run the experiment that would establish the mechanism.

Stage C alone is nominally negative on five of seven BERT tasks and
significantly negative on SciFact ($-1.95$, $2/13$) on both tests uncorrected,
though not under the tightened $\alpha=0.00143$.

We do not read this as overturning the GLUE results --- the training regime is
different in kind (contrastive on MS MARCO then zero-shot, against supervised
fine-tuning on the target task), and the same arm can reasonably behave
differently under the two. What it does overturn is the \emph{scope} of the
claim, and \S\ref{sec:stageB} is restated accordingly.

\section{Notes for reproducers}
\label{app:defects}

Five details of the released code cost us time. The first three do not affect
the conclusions of the original paper; the last two are asymmetries between our
own arms.

\begin{itemize}
\itemsep1pt
\item \texttt{jk\_mode} is advertised as $\{$\texttt{cat}, \texttt{max},
      \texttt{mean}, \texttt{none}$\}$, the CLI accepts $\{$\texttt{cat},
      \texttt{lstm}, \texttt{max}$\}$, and the pooler implements
      $\{$\texttt{cat}, \texttt{max}, \texttt{mean}$\}$. \texttt{lstm} parses
      and then raises at runtime; \texttt{mean} is implemented but unreachable
      from the CLI. The usable space is $\{$\texttt{cat}, \texttt{max}$\}$.
\item Thresholding Poincar\'e \emph{distance} at a fixed radius gives an empty
      graph on real features (see \S\ref{sec:method}). The signature is a score
      that is bit-identical across a whole threshold grid. We use a quantile
      threshold everywhere for this reason.
\item The logged edge-density counts self-loops in the numerator but excludes
      the diagonal from the denominator, so an \emph{empty} graph logs
      $1/(n{-}1) \approx 0.10$ at CoLA's typical length rather than $0$. This is
      a reporting defect only --- no training path reads the statistic --- but
      it is a trap for exactly the audit this paper performs. It was corrected
      partway through the project, so the CoLA logs carry the offset and the
      later MRPC logs do not; densities are not comparable across campaigns
      without checking. Any density we quote is re-measured with the standalone
      instrument of Table~\ref{tab:density}, never taken from training
      telemetry.
\item \textbf{Our Stage C layers add self-loops twice.} The graph builder emits
      self-loops, and \texttt{HyperbolicGCNConv}/\texttt{HyperbolicGATConv} then
      call \texttt{add\_self\_loops} without removing existing ones first, so
      every node gets two. PyG's Euclidean \texttt{GATConv}, which the baseline
      uses, removes before adding. Measured on the real BERT/CoLA cache ($105$
      nodes, $587$ edges) the hyperbolic layers see $692$ edges where the
      Euclidean one sees $587$. Forward-pass effect at Stage C's tuned
      configuration: relative output change $2.38\times10^{-2}$ at $q{=}0.38$
      and $1.92\times10^{-2}$ at $q{=}0.10$, against $4.13\times10^{-1}$ for a
      dense-to-sparse graph change and exactly $0$ for the same call twice. It
      is small, but it is real, and because it differs between the two
      hyperbolic cells it does not cancel in the ``geometry alone'' contrast of
      Table~\ref{tab:decomp}. The layer is now fixed
      (\texttt{remove\_self\_loops} before \texttt{add\_self\_loops};
      \texttt{test\_edge\_parity.py} verifies $105$ against $105$ on the real
      graph) and Table~\ref{tab:decomp} is measured with the fix in place.
\item \textbf{And the Euclidean control consumes an edge attribute the
      hyperbolic one ignores.} \texttt{HyperbolicGATConv} accepts
      \texttt{edge\_weight} and never uses it, while the Euclidean
      \texttt{GATConv(edge\_dim=1)} does consume it. At the
      \texttt{binary} default every attribute is exactly $1.0$, which looks
      inert --- and an earlier revision of this appendix asserted it was,
      claiming ``no reported comparison is affected''. \emph{That claim was
      wrong.} \texttt{GATConv} forms
      $\mathrm{leakyrelu}(\alpha_{\text{src}} + \alpha_{\text{dst}} +
      \alpha_{\text{edge}})$, and a leaky ReLU is not translation-equivariant,
      so a constant does \emph{not} cancel in the neighbourhood softmax. On the
      real $105$-node CoLA graph, deleting the path changes the layer output by
      $1.36\times10^{-2}$ relative --- $1.2\%$ of a dense-to-sparse graph swap,
      against a noise floor of exactly $0$, and comparable to the self-loop
      defect above. The flag never varies, but the \emph{path} is present in
      every Euclidean arm and absent from every hyperbolic one, so this is not a
      dormant knob: it is a second respect in which every Euclidean-versus-
      hyperbolic contrast in this paper differs by more than curvature. It also
      costs $256$ parameters ($98{,}944$ against $98{,}688$). A
      \texttt{--gat\_edge\_attr} flag now removes it, bringing the Euclidean
      layer to $98{,}688$ parameters against the hyperbolic layer's $98{,}689$.
\end{itemize}

\section{Audit trail for this revision}
\label{app:audit}

Every table was recomputed from the campaign CSVs. Fourteen values changed. They
are listed here rather than corrected silently, because several of them are the
kind of error this paper criticises in others.

\begin{itemize}
\itemsep1pt
\item RTE's minimum detectable effect was quoted as $2.7$ accuracy points; from
      the wide campaign's own paired scatter it is $2.064$. It appeared in five
      places. The conclusion drawn from it --- RTE cannot resolve any effect in
      this paper --- is unchanged.
\item The pooled share of tuning trials that fail to train was quoted as
      $19.2\%/2.2\%/0\%$ across the three learning rates; recomputed over all
      $2{,}320$ de-duplicated tuning rows it is $24.3\%/3.5\%/0.1\%$. The
      per-task figures ($81$ of $121$ on BERT/CoLA, $66.9\%$) were correct. The
      RoBERTa/CoLA figure was quoted as $44.1\%$ and is $71.5\%$.
\item Four corrections were arithmetic or labelling: two standard deviations in
      Table~\ref{tab:factorial} ($1.42 \rightarrow 0.90$,
      $2.19 \rightarrow 2.05$); a population-versus-sample standard deviation
      mix in Table~\ref{tab:fix}, now uniformly sample; the decoder baseline
      seed standard deviation ($0.48 \rightarrow 0.494$); and a
      noise-diagnostic row labelled ``A'' when two Stage A variants exist, both
      of which are now shown.
\item The same table omitted \texttt{no graph}, \textsc{Glot} $K{=}0$ and ABC.
      \texttt{no graph} is the best arm at three of four ratios, which reverses
      the paragraph that accompanied the table.
\item ``Stage A is ahead in the noise diagnostic at three of four ratios'' was
      wrong in our own favour: it is ahead of the \emph{baseline} at four of
      four, and behind \texttt{no graph} at three of four.
\item ``On MRPC every hyperbolic arm is slightly negative'' was wrong: Stage A
      is $+0.04$. The claim now names the arms containing B or C.
\item The paper stated that the \texttt{no graph} control had never been run on
      BERT and listed it as an unscheduled experiment. It had been run, on all
      four BERT tasks and on the decoder, and it is mildly positive on BERT ---
      which strengthens rather than weakens the surrounding argument.
\item Table~\ref{tab:docmteb}'s caption claimed its seven tasks were those of
      \citet{mantri2026glot}. They are from that paper's appendix Table 12, not
      its Table 3; only two of seven overlap.
\item The RoBERTa \texttt{published\_tau} arm is significant on both tests
      uncorrected on STS-B ($+0.32$, $t=3.13$, $p=0.035$) and was not marked.
\item ``Jumping-knowledge mode is uniform within a task'' holds on CoLA and
      STS-B and fails on MRPC and RTE, where one arm each departs.
\item The matched-estimator sweep of Table~\ref{tab:repro} was described as
      $78$ configurations per task. The three sweep files hold $68$ each. The
      three scores themselves ($50.52$, $59.57$, $82.99$) reproduce exactly, so
      only the budget was misstated --- but it was misstated in our favour,
      since a larger claimed grid makes the matched comparison look more
      thorough than it was.
\item Most consequentially, the $2\times2$ of Table~\ref{tab:factorial} spliced
      two cells from one campaign into two from another. The factorial has since
      re-run the shared cell and disagrees with the original on all $15$ seeds
      (Appendix~\ref{app:determinism}), which moved the ``geometry alone''
      component of Stage C's gain from $+0.40$ to $+0.07$ --- and the $65$-seed
      run has since moved it again, to $+0.534$ (see below). All three figures
      describe the same contrast and differ only in how the shared cell was
      obtained.
\end{itemize}

Everything else reproduced exactly: all $8$ arms $\times$ $6$ task-backbone
columns of Table~\ref{tab:absolute}, every delta, $t$, sign-$p$ and seed split
in Tables~\ref{tab:verdict} and~\ref{tab:roberta}, every row of
Table~\ref{tab:hp}, the re-measured densities of Table~\ref{tab:density}, the
decoder block, the IMDB and MTEB values, and the learning-rate selection claims.

\subsection*{What the later runs changed}

Nine queued jobs reported after the audit above, in two waves. Each is recorded
here because most of them moved a claim, and several moved it against us.

\begin{itemize}
\itemsep1pt
\item \textbf{Stage A on CoLA at $n=50$ turned an unresolved trend into a
      positive} (\S\ref{sec:stageA}), which previously read ``neither a positive
      nor a null''. The $15$ seeds carried over from the wide campaign and the
      $35$ newly run agree, and every pair is within one campaign.
\item \textbf{Stage A then lost its clean record.} The RoBERTa MTEB block found
      it significantly negative on six of seven retrieval tasks, four at $0/15$
      seeds surviving Bonferroni. The previous revision asserted that Stage A
      ``never appears on the negative side''; that is now false, and the tally
      paragraph of Appendix~\ref{app:results} says so.
\item \textbf{The corrective factorial closed Stage C's attribution.} The
      $n{=}65$ factorial was first run with two control-parity defects open
      (Appendix~\ref{app:defects}); rerunning all $260$ cells with both closed
      moved the geometry term from $+0.534$ to $+0.502$ and left the conclusion
      unchanged. Against the $n=15$ version the interaction the run was
      commissioned to detect collapsed from $+1.362$ to $+0.224$.
\item \textbf{The decoder's B and C arms were rerun in a matched campaign.}
      Their first run wrote to a file containing no baseline, so their deltas
      spliced two campaigns --- the practice this appendix lists above as the
      most consequential error of the previous revision, repeated by us one
      revision later. The matched rerun reproduced all three values to two
      decimals, so the splice was accurate; we would still refuse to rely on it
      (Appendix~\ref{app:decoder}).
\item \textbf{The decoder's CoLA column produced the largest fine-tuning effects
      in the paper}, $-10.26$ MCC for B and $-13.58$ for AB, and revealed that
      two of AB's fifteen seeds fail to train at all.
\item \textbf{SST-2 removed the small-data confound, against us.} The previous
      revision listed it as an open threat to the paper's firmest result. It
      was: at $67{,}349$ training examples Stage B is $+0.65$ accuracy rather
      than negative, and every Stage B claim is now scoped to small tasks
      (Appendix~\ref{app:sst2}).
\item \textbf{The MTEB job had failed on a path bug, not a pipeline fault}, and
      the relaunched RoBERTa block was then skipped entirely by a
      model-name mismatch that spent no compute and stopped nothing
      (Appendix~\ref{app:mteb3}). Both are our defects, not the original's.
\item \textbf{We do not reproduce the published MTEB absolutes.} Checking the
      completed blocks against \citet{mantri2026glot}'s Table 3 showed the
      published value above our best of $15$ seeds on twelve of fourteen encoder
      cells, and RoBERTa SciFact degenerate at $0.58$ against $26.05$. This was
      not known when the BERT block was first written up.
\item \textbf{Four cost figures were wrong and are corrected.} The uncovered
      GLUE tasks were quoted at $170$ hours for SST-2, $264$ for QNLI and $38$
      and $41$ \emph{days} for QQP and MNLI. Those scaled linearly from full
      training-set size and ignored the original's \texttt{train[:20000]} cap
      (their B.3), which makes exactly those three tasks cheap. Refitted on our
      four measured tasks they are $108$, $83$, $185$ and $124$ \emph{hours},
      and the companion cache figure is $75$\,GB rather than $149$.
\end{itemize}

\section{Does Stage B's harm survive more data?}
\label{app:sst2}

Stage B hurts by amplifying seed variance, and variance is a quantity more data
suppresses. Every task in Table~\ref{tab:absolute} has at most $8{,}551$
training examples, so the negative could be a small-data artifact rather than a
property of the readout. SST-2 is the cheapest task that separates the two: it
has $67{,}349$ training examples, $7.9\times$ CoLA, and is \emph{not} one of the
tasks the original caps at \texttt{train[:20000]}.

\emph{This is a weaker design than the rest of the paper and we report it as
one.} The four arms replay the configurations selected on CoLA rather than
tuning on SST-2, so the comparison cannot rank arms. It can answer the one
question it was built for --- whether B's harm persists at eight times the data
--- because the reduction is applied identically to the baseline and to the
treatment arms.

\begin{table}[h]
\caption{SST-2 accuracy, mean $\pm$ sd over the $15$ shared seeds, $\times100$,
with paired differences against the baseline. Configurations are replayed from
CoLA, so these numbers rank nothing; they are reported only against the
small-data confound. No arm is significant on both tests.}
\label{tab:sst2}
\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
Arm & mean & $\Delta$ & p/n & $t$ \\
\midrule
\textsc{Glot} (baseline) & 89.57\,$\pm$0.45 & \emph{ref} & --- & --- \\
B  & \textbf{90.22}\,$\pm$0.66 & $+0.65$ & 10/5 & $2.98$ \\
C  & 89.96\,$\pm$0.65 & $+0.39$ & \phantom{0}9/6 & $1.99$ \\
BC & 89.83\,$\pm$0.60 & $+0.26$ & 10/5 & $1.29$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\textbf{The harm does not survive.} Stage B runs $-1.07$ on BERT STS-B, $-1.26$
on RoBERTa STS-B, $-1.16$ on the decoder's STS-B and $-10.26$ on the decoder's
CoLA. On SST-2 it is $+0.65$. That is resolved by the $t$-test and not by the
sign test ($10/5$, $p=0.302$), so by our own both-tests rule it is a null, not a
gain --- but it is emphatically not the negative the small tasks show, and the
minimum detectable effect here is $0.431$, so the column is not merely
underpowered.

The mechanism moves in step. Stage B raises the seed standard deviation by
$1.98\times$ on the decoder's STS-B and $1.69\times$ on the decoder's CoLA; here
it is $1.47\times$, and BC's standard deviation ($0.60$) is barely above the
baseline's ($0.45$) where on every small task adding B raised it sharply. The
whole four-arm spread on SST-2 is $0.65$ accuracy points, against up to $13.58$
MCC on the decoder's CoLA.

We therefore scope every Stage B claim in this paper to supervised fine-tuning
on \emph{small} target tasks. What we cannot say is where the boundary lies: we
have one task above $8{,}551$ examples and one below, which locates the
transition somewhere in an order of magnitude and no more precisely.

\section{Limitations}
\label{app:limitations}

\begin{itemize}
\itemsep1pt
\item \textbf{Stage C's gain is real but not attributable to geometry.} The
      matched $2\times2$ at $n=65$ (Appendix~\ref{app:factorial2x2}) splits the
      gain into geometry, configuration and interaction. The total is
      significant on both tests; the curvature term is not significant on
      either, and the configuration term is more than twice its size.
      Resolving the interaction at its observed size would need roughly
      $2{,}000$ seeds, which we will not run.
\item \textbf{Stage B's harm is bounded by regime and by scale, and we found
      both out late.} The same readout that is reliably harmful under
      fine-tuning on small tasks appears in the arms producing the largest
      positive effects in this paper under contrastive training with zero-shot
      evaluation
      (Appendix~\ref{app:mteb3}), and is no longer distinguishable from the
      baseline once the task has $67{,}349$ training examples
      (Appendix~\ref{app:sst2}). We have no account of which task property
      predicts the sign, and with seven retrieval tasks we cannot get one. Every
      Stage B statement here is scoped to supervised fine-tuning on small
      target tasks.
\item \textbf{Our MTEB absolute numbers do not reproduce the original's, and
      one cell is broken.} The published values exceed our best of $15$ seeds on
      twelve of fourteen encoder cells, and RoBERTa SciFact scores $0.58$
      against a published $26.05$. Every delta we report is paired within our
      own campaign and so does not depend on the level, but the two SciFact
      columns rest on a degenerate baseline and we exclude them from effect-size
      comparisons (Appendix~\ref{app:mteb3}).
\item \textbf{Stage A's positive is one task deep.} It separates on CoLA at
      $n=50$ ($+0.83$, both tests) and in none of the other ten fine-tuning
      settings, and it is significantly negative on six of seven RoBERTa
      retrieval tasks. We have not tested whether the CoLA positive survives on
      a second task at the same seed budget.
\item \textbf{A seed does not identify a run} (Appendix~\ref{app:determinism}).
      Within-campaign paired tests are unaffected, but pairing recovers much
      less variance than we claimed on CoLA, and no table may splice cells
      across campaigns. We did not discover this until the audit for this
      revision.
\item \textbf{Our Stage C control differed from its treatment in two ways that
      are not curvature} (Appendix~\ref{app:defects}). The hyperbolic layers
      duplicated self-loops, and the Euclidean GAT consumed an edge attribute
      the hyperbolic GAT ignores --- which is not inert, because the attribute
      enters before a leaky ReLU. Together they are about $6\%$ of a
      dense-to-sparse graph change. Both are fixed, and the factorial of
      Table~\ref{tab:decomp} was rerun with the fixes in place; the geometry
      term moved by $0.032$ MCC.
\item \textbf{We select the epoch on the split we report}, because GLUE test
      labels are not public. This is a smaller version of the practice we
      criticise in \S\ref{sec:choices}. Splitting the development set into
      selection and reporting halves would remove the objection, and is the
      single most valuable improvement to this protocol.
\item \textbf{One reproduction residual is unexplained.} STS-B stays $0.87$
      short under every estimator we tried; we tested and ruled out the leading
      hypothesis (Appendix~\ref{app:factorial}).
\item \textbf{The scale factorial is underpowered.} At $n=5$ the exact sign test
      cannot fall below $p = 0.0625$, so no contrast in Table~\ref{tab:fix} can
      meet our own bar.
\item \textbf{Non-Euclidean layers may need structure-aware optimisation.} The
      search moved the learning rate per arm and per task rather than pinning it
      to a flat-tuned value, but we did not implement per-manifold Riemannian
      optimisation or curvature-aware initialisation. This is the strongest
      available objection to our Stage B negative, and we state it rather than
      merely acknowledging it.
\item \textbf{Every fine-tuning task in our main tables is small, and that is
      most of why Stage B looks harmful.} CoLA, STS-B, MRPC and RTE have
      $8{,}551$, $5{,}749$, $3{,}668$ and $2{,}490$ training examples. SST-2 has
      $67{,}349$, and there Stage B is not distinguishable from the baseline
      (Appendix~\ref{app:sst2}). That design is weaker than the others by
      construction --- no arm is tuned on SST-2 --- so it can say whether the
      harm persists at scale but cannot rank arms, which is why its numbers stay
      in that appendix and out of Table~\ref{tab:absolute}.
\item \textbf{\textsc{Bert} RTE is underpowered and contributes nothing.} Its
      minimum detectable effect of $2.06$ accuracy points exceeds every effect
      in that block, and no arm in it is significant. We include the column for
      completeness and base no conclusion on it. The decoder's RTE column is a
      different matter and carries its own caveat
      (Appendix~\ref{app:chance}).
\item \textbf{Coverage.} We never ran Mean, Max, \texttt{[CLS]} or AdaPool on
      GLUE, so Table~\ref{tab:absolute} reproduces the \emph{layout} of the
      original's Table 1 but not its content; our rows are \textsc{Glot}'s own
      components. What we did run is listed in \S\ref{sec:porting}.
\item \textbf{The stages replicate; their interaction does not.} Stage B's harm
      reproduces across two encoders and a decoder, but the sign of the
      B$\times$C interaction differs between all four encoder settings
      (\S\ref{sec:interaction}) and at the extremes is Bonferroni-significant in
      both directions. We have no account of why. This bounds what a
      component-wise study can deliver: individual stages generalise, but a
      two-stage combination has to be measured where it will be used.
\end{itemize}

\end{document}
