\documentclass{article}
\usepackage{iclr2026_conference,times}

% Short form. The body is prose only -- roughly four pages -- and carries the
% question, the method, the choices that changed the answer, and the findings.
% Every table, the figure, and all supporting measurement live in the appendix.

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xcolor}

\newcommand{\PEND}{\textcolor{gray}{--}}

\title{Does Hyperbolic Geometry Help Graph-Based\\
Sentence Pooling? A Controlled Stage-wise Study}

\author{Anonymous authors\\
Paper under double-blind review}

\begin{document}

\maketitle

\begin{abstract}
Hyperbolic space stores tree-like data with less distortion than flat space.
Token states are often called tree-like. Together these have motivated
hyperbolic versions of many models. We test that reasoning on one model,
carefully.

Our subject is \textsc{Glot} \citep{mantri2026glot}, a sentence pooler that
builds a graph over the tokens of a frozen language model, refines it with a
GNN, and sums it into one vector with a learned readout. Geometry enters in
exactly three places, so we build a hyperbolic version of each and test them
one at a time instead of together: a Poincar\'e graph (\textbf{A}), an Einstein
gyro-midpoint readout (\textbf{B}), and M\"obius message passing (\textbf{C}).
All eight combinations get the same search budget on the original model's own
grid, with 15 shared seeds, on four GLUE tasks, a decoder backbone, and a
noise test.

The components disagree. The readout (B) is clearly harmful: on STS-B it loses
$0.75$--$1.24$ Spearman with $0$ or $1$ of $15$ seeds positive, all four arms
that contain it survive Bonferroni, and it repeats on a decoder backbone where
it costs $3.6$ Spearman. It is negative on CoLA and MRPC too, and positive only
on RTE, which is too small to resolve any effect we measure. Message passing
(C) is the only component that ever helps significantly, by $+1.42$ MCC on
CoLA. The graph (A) does nothing anywhere. Because B and C point in opposite
directions, a study that reported ``hyperbolic pooling'' as a single switch
would have averaged them to about zero.

We are cautious about C. Its gain does not reach MRPC, and the search gave it a
much denser graph and a different network size than the baseline, so the number
mixes geometry with those choices.

We also question the premise. The usual reason to go hyperbolic is a low
measured $\delta$-hyperbolicity. That measure is an artifact of a few
large-norm tokens: on an outlier-free angular metric ModernBERT scores $0.2127$
and BERT $0.2114$ --- the same --- while the Euclidean version of the same
measure differs between them by two orders of magnitude.
\end{abstract}

%-------------------------------------------------------------------------
\section{Introduction}

Turning a language model's token states into one vector is a first step for
most sentence tasks. Simple poolers --- mean, max, \texttt{[CLS]} --- treat
tokens as an unordered bag. A newer idea is to treat pooling as \emph{relations
first, aggregation second}: build a graph over tokens, refine it with a GNN,
then aggregate. \textsc{Glot} \citep{mantri2026glot} is a strong example,
matching competitive GLUE and MTEB numbers with $20\times$ fewer trained
parameters and a frozen backbone. Our runs support the claim it rests on: on
the authors' own noise test, \textsc{Glot} beats every bag-of-tokens pooler by
$14$--$35$ points in every seed.

This paper asks what else that design can carry. Hyperbolic space is a natural
candidate for all three of its parts, so we build a hyperbolic version of each
and measure them separately. That separation is the point: two of the three
move in opposite directions, so a single ``hyperbolic'' switch would have
reported their average and found nothing.

\paragraph{Contributions.}
\begin{enumerate}
\itemsep1pt
\item \textbf{A stage-wise hyperbolic study} (\S\ref{sec:results}). Under an
      equal search budget with 15 shared seeds, the hyperbolic readout (B) is
      significantly harmful on an encoder and on a decoder; hyperbolic message
      passing (C) is the only part that ever helps; the hyperbolic graph (A) is
      flat everywhere. We report what each part does, not what they do on
      average.
\item \textbf{The usual motivation does not survive measurement}
      (\S\ref{sec:results}). Low $\delta$-hyperbolicity is driven by a handful
      of very large token norms. On an outlier-free metric the backbones it is
      used to tell apart are indistinguishable. So measured tree-likeness
      cannot tell you where curvature will help.
\item \textbf{The choices that changed our answers} (\S\ref{sec:choices}). A
      cache-warming order worth $5$ MCC, a search that reverses arm rankings, a
      threshold that silently produces a complete graph on RoBERTa, and two
      defects in the released code. We list these because each of them cost us
      an experiment.
\end{enumerate}

%-------------------------------------------------------------------------
\section{What we changed}
\label{sec:method}

Let $x_1,\dots,x_L$ be the token states of a frozen backbone. \textsc{Glot}
joins two tokens when their cosine passes a threshold,
\begin{equation}
A_{ij} = \mathbf{1}\!\left[\, \cos(x_i, x_j) > \tau \,\right],
\label{eq:adj}
\end{equation}
runs $K$ graph-attention layers over the result, and aggregates with a learned
readout. Only the pooler trains. Geometry enters in three places --- how the
graph is built, how messages combine, how states are aggregated --- so we
replace each in turn. Appendix~\ref{app:math} gives the Poincar\'e ball, the
maps $\exp^c_0$ and $\log^c_0$, and the geodesic distance we use.
Figure~\ref{fig:overview} in Appendix~\ref{app:fig} shows where each stage sits.

\paragraph{Stage A: a Poincar\'e graph.} Lift the tokens into the ball and join
them when their geodesic distance falls below the $q$-quantile of all pairwise
distances,
$A_{ij} = \mathbf{1}[\,d_c(\exp^c_0(\tilde{x}_i), \exp^c_0(\tilde{x}_j)) < \rho_q\,]$.
Using a quantile rather than a fixed radius fixes the edge count at $q$, so A
and the cosine baseline can be compared at the same sparsity.

\paragraph{Stage B: an Einstein gyro-midpoint readout.} Replace the weighted
sum with the Einstein midpoint in Klein coordinates,
\begin{equation}
m_K = \frac{\sum_i a_i \gamma_{k_i} k_i}{\sum_i a_i \gamma_{k_i}},
\qquad k = \frac{2x}{1 + c\|x\|^2},
\qquad \gamma_k = (1 - c\|k\|^2)^{-1/2},
\label{eq:midpoint}
\end{equation}
then map back and into the tangent space for the task head. Unlike a plain mean
this depends on curvature: as $c \rightarrow 0$ it becomes the arithmetic mean.

\paragraph{Stage C: M\"obius message passing.} Replace each GNN layer with a
hyperbolic one that lifts to the tangent space to combine neighbours,
\begin{equation}
h_i' = \exp^c_0\!\Big(\sigma\Big(\textstyle\sum_{j \in \mathcal{N}(i)}
       \alpha_{ij}\, \log^c_0\big(W \otimes_c h_j\big)\Big)\Big),
\end{equation}
with $W \otimes_c h = \exp^c_0(W \log^c_0(h))$ and $\alpha_{ij}$ either
degree-based (HGCN) or attention (HGAT).

\paragraph{Curvature is searched in a scale-free way.} Curvature and feature
scale cannot be told apart here. The geodesic distance depends only on
$\sqrt{c}\,\|x\|$, so doubling the features and quartering $c$ leaves every
distance unchanged, and searching $c$ on its own wastes most of the grid. We
instead rescale features to unit mean norm and search the \emph{effective
radius} $r_{\text{eff}} = \sqrt{c}\,\mathbb{E}\|x\|$ over
$\{0.5, 1, 1.5, 2, 3\}$. This is not cosmetic: with raw BERT token norms
($\approx 14.7$) the map $\exp^c_0$ saturates to radius $1.0$ in
\texttt{float32}, real pairwise distances land in $[8.85, 11.76]$, and any
fixed distance threshold below about $9$ returns an empty graph
(Appendix~\ref{app:defects}).

We run all eight combinations of A, B and C.

%-------------------------------------------------------------------------
\section{How we ran it, and the choices that changed the answer}
\label{sec:choices}

Every arm is frozen-backbone, 2 epochs, Adam, batch $32$; only the pooler
trains. Tuning draws $40$ configurations at one held-out seed. Confirmation
re-runs the winner on the fixed seed set $\{1,\dots,15\}$, shared across arms,
so every comparison is paired. We report the exact two-sided sign test beside
the paired $t$-test and call something significant only when both agree, with a
Bonferroni correction across arms ($\alpha = 0.0063$). Full protocol in
Appendix~\ref{app:protocol}. Five choices moved our results more than the
effects we were trying to measure.

\paragraph{The search space must be the same for every arm.} An earlier
campaign of ours searched only graph knobs and left the learning rate, depth and
width pinned at values picked for a \emph{flat} pooler. That handed the baseline
an advantage and produced a false positive we retract in
Appendix~\ref{app:narrow}: Stage A on STS-B looked like $+0.223$ Spearman with
$15/15$ seeds and $t(14)=6.20$, and collapsed to $+0.028$ once the baseline was
allowed to pick $K=4$. Everything reported here uses \textsc{Glot}'s full grid
applied identically to all arms: $40$ trials each, $15$ seeds, $990$ runs.

\paragraph{The search really does use that freedom.} The learning rate does not
settle on one value. On STS-B all nine arms chose $2\times10^{-4}$; on CoLA
eight of nine chose $1\times10^{-3}$; MRPC and RTE split. Depth, width and
threshold move too. Arms were therefore not locked to a rate chosen for a flat
pooler, which is the main form of the objection that non-Euclidean layers get
judged under flat-tuned optimisers. Appendix~\ref{app:hp} lists the selected
configurations.

\paragraph{Tuning maxima are inflated, and can reverse a ranking.} With $T$
trials and per-seed noise $\sigma$, the best of $T$ sits above the truth even
for an arm identical to the baseline --- about $1.83$ here on STS-B. On the
RoBERTa campaign the inflation is large enough to flip the order outright: the
quantile correction is first of five at the tuning maximum ($56.77$) and last
of five over 15 seeds ($53.19$). Reporting tuning maxima would have inverted our
conclusion. We report confirmation means only.

\paragraph{Cache order is worth about $5$ MCC.} The released code caches
backbone states and builds the shuffled loader \emph{before} the classifier is
created. On a cache miss the loader consumes the global RNG; on a hit it does
not. The same seed therefore gives a different initialisation depending on cache
state --- $40.37$ cold against $45.54$ warm on CoLA, about six times the seed
standard deviation. We build all caches before any arm runs. Any reproduction
that skips this will shift by several points.

\paragraph{The estimator matters as much as the method.} \citet{mantri2026glot}
fix seed $42$ and report the best $\tau$ per task on the split they also report,
so every published number is one draw maximised over a search. We report 15-seed
means. On CoLA the choice of estimator alone moves the same runs by nearly $5$
MCC (Appendix~\ref{app:repro}). Matching their estimator exactly, we reach
$50.52$ MCC on CoLA against a published $47.49$ and $59.57$ on RTE against
$59.21$; STS-B stays $0.87$ short and we could not explain it. Every comparison
in this paper is against our own baseline under identical conditions, never
against a published number.

%-------------------------------------------------------------------------
\section{What we found}
\label{sec:results}

Appendix~\ref{app:results} holds the numbers; this section states what they say.

\paragraph{The hyperbolic readout is harmful, and this is the firmest result in
the paper.} Every arm containing Stage B loses $0.75$--$1.24$ Spearman on STS-B,
with $0$ or $1$ of $15$ seeds positive. B alone gives $t = -17.00$ with $0/15$
seeds and a sign-test $p$ of $6\times10^{-5}$, the smallest value $15$ paired
seeds can produce. All four arms containing B survive Bonferroni. The result
repeats on a decoder backbone, TinyLlama, where AB costs $3.56$ Spearman and ABC
costs $4.51$, both $0/15$.

There is a mechanism, not just a mean. On the decoder the seed standard
deviation rises in step with the number of hyperbolic parts:
$0.48 \rightarrow 0.95 \rightarrow 1.87 \rightarrow 3.58$, a $7.5\times$
spread. The gyro-midpoint behaves like a variance amplifier rather than a useful
prior. It is also the one component whose theoretical motivation is weakest
here: pooling averages tokens that the graph has already smoothed, and the
Einstein midpoint's curvature dependence pulls hardest exactly on the
large-norm tokens that Appendix~\ref{app:calib} shows are outliers.

One task disagrees, and we state it rather than bury it. On RTE all four
B-containing arms are \emph{positive} ($+0.79$ to $+1.85$ accuracy). None is
significant, and none could be: RTE has $277$ evaluation examples, a seed
standard deviation of $3.36$, and a minimum detectable effect of $2.7$ points
--- larger than any effect reported in this paper. We read RTE as uninformative
rather than as contrary evidence, but it does mean the honest claim is ``B is
harmful on the three tasks that can measure it'', not ``B is harmful
everywhere''.

\paragraph{Message passing is the only part that ever helps.} Stage C reaches
$+1.42$ MCC on CoLA with $13/15$ seeds positive ($t = 4.11$, sign $p = 0.0074$)
--- significant on both tests, though just above our Bonferroni line of
$0.0063$. On STS-B, A${+}$C is the best of the nine arms ($+0.24$) and C is
second ($+0.20$). Every arm with the hyperbolic GNN and \emph{without} the
readout is positive on both tasks.

We do not present this as settled, for two reasons. It does not reach MRPC,
where no arm beats the baseline and every hyperbolic arm is slightly negative
($-0.15$ to $-0.33$, none significant). And the search did not give C the same
graph or the same network as the baseline: it chose a threshold of $q = 0.38$
against the baseline's $0.10$, a $3.6\times$ denser graph, together with half
the depth and half the width. The $+1.42$ therefore mixes geometry with those
choices. It also fails the sharpest control we have: an arm that deletes every
edge scores $46.20$ on CoLA, and C does not separate from it ($+0.58$, $8/7$,
$p = 1.00$). We are running the matched comparison that isolates the geometry
(Appendix~\ref{app:limitations}).

\paragraph{On these tasks the edges are doing less than expected.} The
graph-free arm is not worse than the baseline on any of the four tasks (CoLA
$4/11$, STS-B $8/7$, MRPC $6/9$, RTE $7/6$), and on CoLA it ranks second of
eight. It is still a trained pooler, so this is not a claim that
\textsc{Glot}'s design is unnecessary --- against real bag-of-tokens poolers the
graph wins by $14$--$35$ points on the authors' noise test. The narrower claim
is that on GLUE, at $40$ trials per arm, accuracy does not come from the edges.
That matters here because it sets the bar every geometric claim must clear:
Stage C clears it on STS-B and not on CoLA, and Stage B is significantly
\emph{below} it.

\paragraph{The graph is flat, and so is the choice of GNN.} Stage A moves
nothing anywhere: $+0.70$ on CoLA, $+0.03$ on STS-B, $+0.04$ on MRPC, $+0.04$
on the decoder, none significant. Swapping GAT for GCN or GIN moves CoLA by at
most $0.52$ MCC and STS-B by $0.23$, inside seed noise. Neither the geometry of
the graph nor the architecture that reads it is where the variation lives.

\paragraph{The components cancel.} B and C carry opposite signs. A study that
turned ``hyperbolic'' on as one switch would have reported their average, which
is close to zero, and concluded that geometry does not matter here. It does ---
the parts simply disagree, and only a stage-wise test can see that.

\paragraph{The motivation is an artifact.} Hyperbolic layers are usually
justified by measuring Gromov $\delta$-hyperbolicity and finding it small. On
the Euclidean metric ModernBERT at layer 16 gives $\delta = 0.0021$, which reads
as almost perfectly tree-like. But that layer has a maximum-to-median token norm
ratio of $156$, and $\delta$ is scale-sensitive: a few very large ``attention
sink'' tokens dominate it and pull it toward zero. On the angular metric
$\arccos(\cos(\cdot,\cdot))$, which ignores those norms, ModernBERT gives
$0.2127$ and BERT gives $0.2114$ --- indistinguishable, while the Euclidean
numbers differ by two orders of magnitude. Flan-T5 shows the same pattern with
$357\times$ norm outliers. So measured tree-likeness cannot tell you which
backbone or which component will benefit from curvature. We did not use it to
choose; we applied all three stages everywhere and let the paired tests decide.

\paragraph{Porting the method to a new backbone.} Equation~\ref{eq:adj} compares
a cosine to a fixed number, which only makes sense if cosine scale is comparable
across models. It is not. On RoBERTa the \emph{tenth}-percentile token cosine is
$0.693$, above the largest $\tau$ in the published grid, so every searched
setting yields an essentially complete graph ($0.991$ density). The useful
finding is what happens next: \textsc{Glot} is unharmed by that dense graph,
while the obvious repair --- switching to a quantile threshold --- costs $1.41$
MCC and survives Bonferroni. It also won its tuning stage before finishing last
over 15 seeds, so selection actively favoured it. We therefore withdraw
density-matching as a recommendation for anyone porting this method.

\paragraph{Scope, and one confound it creates.} \citet{mantri2026glot} report ten
GLUE tasks. We report four (CoLA, STS-B, MRPC, RTE), plus RoBERTa, ModernBERT,
SmolLM2 and TinyLlama in supporting roles. The four we chose are the four
\emph{smallest}, and not by accident: our design costs $495$ runs per task,
which is $22$ hours for CoLA but an estimated $38$ and $41$ days for QQP and
MNLI, with $149$\,GB of cached states between them.

That leaves a confound we cannot currently rule out. Every task here has at most
$8{,}551$ training examples, and Stage B's failure mode is variance
amplification --- a quantity that shrinks as data grows. Our Stage B negative
may therefore be a small-data effect rather than a property of the geometry.
SST-2 ($67$k examples, $7.9\times$ CoLA) is the cheapest test that would settle
it, and we did not run it. We also report no MTEB or IMDB numbers.

%-------------------------------------------------------------------------
\section{Conclusion}

Whether hyperbolic geometry helps graph-based pooling depends entirely on
\emph{which part} receives it, and the parts disagree enough to cancel.

The Einstein gyro-midpoint readout is reliably harmful: negative in every
setting we ran, $0$ or $1$ of $15$ seeds positive on STS-B, surviving Bonferroni
on an encoder and repeating on a decoder, with a variance signature that
suggests why. M\"obius message passing is the one part that ever helps, by
$+1.42$ MCC on CoLA, but it does not reach MRPC and the comparison is not yet
clean. The Poincar\'e graph does nothing at all.

Two lessons travel beyond this architecture. The standard reason to go
hyperbolic --- a low measured $\delta$ --- is an artifact of norm outliers, and
on an outlier-free metric the backbones it distinguishes are the same. And a
comparison between geometries is only as good as the budget given to its
control: our own retracted result, and the confound we found in our headline
number, both come from arms that were not given the same freedom. We report both
so the next study does not repeat them.

\subsubsection*{Reproducibility statement}

All confirmation results use the fixed seed set $\{1,\dots,15\}$, shared across
arms so comparisons are paired; tuning uses one held-out seed. Every run appends
a row to a per-campaign CSV recording the configuration, the realised edge
density, the per-epoch metrics and the wall-clock time. These logs are released
unmodified, including runs that crashed or produced empty graphs, and the
analysis scripts behind every table read those CSVs directly. Caches are built
before any arm runs; that ordering is load-bearing, not an optimisation
(\S\ref{sec:choices}).

\label{page:endprose}

\bibliography{references}
\bibliographystyle{iclr2026_conference}

%=========================================================================
\appendix
\label{page:startappendix}

\section{Hyperbolic preliminaries}
\label{app:math}

We work in the Poincar\'e ball
$\mathbb{B}^d_c = \{x \in \mathbb{R}^d : c\|x\|^2 < 1\}$ of curvature $-c$,
with M\"obius addition $\oplus_c$ and the maps
\begin{equation}
\exp^c_0(v) = \tanh\!\big(\sqrt{c}\,\|v\|\big)\frac{v}{\sqrt{c}\,\|v\|},
\qquad
\log^c_0(x) = \operatorname{artanh}\!\big(\sqrt{c}\,\|x\|\big)\frac{x}{\sqrt{c}\,\|x\|},
\end{equation}
and geodesic distance
$d_c(x,y) = \tfrac{2}{\sqrt{c}}\operatorname{artanh}\big(\sqrt{c}\,\|{-}x \oplus_c y\|\big)$.
With features rescaled to unit mean norm, $c = r_{\text{eff}}^2$.

\section{Architecture}
\label{app:fig}

\begin{figure}[h]
\begin{center}
\includegraphics[width=\textwidth]{glot}
\end{center}
\caption{\textsc{Glot}'s pooling pipeline, reproduced from
\citet{mantri2026glot}. Token states from a frozen backbone are thresholded into
a graph (Eq.~\ref{eq:adj}), refined by a token-level GNN, and aggregated into a
single sentence vector $\mathbf{z}$; only the shaded block trains. Each of our
three changes replaces exactly one depicted component: Stage A the graph, Stage
B the readout, Stage C the token-GNN.}
\label{fig:overview}
\end{figure}

\section{Protocol}
\label{app:protocol}

Frozen backbone, 2 epochs, Adam, weight decay searched, batch size $32$; the
pooler is the only trained component. Confirmation uses seeds $\{1,\dots,15\}$
shared across arms; tuning uses one disjoint held-out seed.

\paragraph{Matched budget.} Every arm gets $40$ trials in the wide campaign
($10$ in the earlier graph-only ones). The cosine baseline has a smaller native
space, so we widen its grid to keep selection pressure equal. Equal trials is
not equal \emph{coverage}: $40$ trials cover $6.2\%$ of a $648$-point space but
under $0.01\%$ of ABC's $4.98\times10^{7}$-point space. This asymmetry works
against the complex arms, and we note it as a caveat against our own negative
results rather than in support of them.

\paragraph{Testing.} We report the exact two-sided sign test beside the paired
$t$-test because evaluation-set quantisation makes the parametric standard error
look too small (MRPC accuracy moves in steps of $0.245$ on $408$ examples). Both
tests must agree. Bonferroni over the $8$ arms tested against a common baseline
gives $\alpha = 0.0063$. The minimum detectable effect at $n=15$ is $0.225$
(STS-B), $1.153$ (CoLA), $0.317$ (MRPC), $2.7$ (RTE) and $0.503$ (TinyLlama).
RTE's figure exceeds every effect in this paper, so that task can only ever
return a null; we report it, and draw nothing from it.

\section{Selected hyper-parameters}
\label{app:hp}

Each arm tunes its own configuration; these are the winners then run on all 15
confirmation seeds. Two things are worth reading off this table. The learning
rate does not converge to a single value across tasks, so no arm was pinned to a
rate chosen for a flat pooler. And on CoLA, Stage C selected a much denser graph
($q = 0.38$) and a smaller network than the baseline ($q = 0.10$, twice the
depth, twice the width) --- the confound discussed in \S\ref{sec:results}. On
MRPC the same search went the other way, giving C a $15\times$ \emph{sparser}
graph than the baseline; C is negative there. C wins where it picked dense and
loses where it picked sparse, which is why the matched comparison is necessary.

\begin{table}[h]
\caption{Confirmed configurations, BERT wide campaigns. ``$q$'' is
\texttt{tau\_quantile} for cosine arms and \texttt{rho\_quantile} for Poincar\'e
arms; both mean ``keep the closest $q$ fraction of token pairs''. ``width'' is
the GNN hidden dimension, ``proj'' the projection dimension.}
\label{tab:hp}
\begin{center}
\small
\begin{tabular}{llccccc}
\toprule
Task & Arm & lr & layers & width & proj & $q$ \\
\midrule
\multirow{5}{*}{CoLA}
 & baseline & $1{\times}10^{-3}$ & 4 & 256 & 128 & 0.10 \\
 & A        & $1{\times}10^{-3}$ & 4 & 256 & 256 & 0.05 \\
 & B        & $2{\times}10^{-4}$ & 2 & 256 & 512 & 0.38 \\
 & C        & $1{\times}10^{-3}$ & 2 & 128 & 256 & \textbf{0.38} \\
 & AC       & $1{\times}10^{-3}$ & 2 & 128 & 256 & 0.05 \\
\midrule
\multirow{5}{*}{STS-B}
 & baseline & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 256 & 0.20 \\
 & A        & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 256 & 0.10 \\
 & B        & $2{\times}10^{-4}$ & 4 & \phantom{0}64 & 128 & 0.38 \\
 & C        & $2{\times}10^{-4}$ & 2 & 128 & 128 & 0.20 \\
 & AC       & $2{\times}10^{-4}$ & 4 & 128 & 512 & 0.20 \\
\midrule
\multirow{3}{*}{MRPC}
 & baseline & $2{\times}10^{-4}$ & 2 & 128 & 128 & 0.38 \\
 & B        & $1{\times}10^{-3}$ & 4 & 128 & 256 & 0.10 \\
 & C        & $2{\times}10^{-4}$ & 2 & 128 & 256 & \textbf{0.025} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Other selected settings vary similarly. Jumping-knowledge mode is uniform
\emph{within} a task but differs \emph{between} them --- \texttt{max} on CoLA,
\texttt{cat} on STS-B --- so it tracks the task, not the arm. Curvature, where
it applies, spans the whole searched range $\{0.25, 1.0, 4.0\}$ with no
consistent preference.

\section{Reproduction}
\label{app:repro}

\begin{table}[h]
\caption{One method, four estimators, on CoLA. Each row is the \emph{same}
pooler; only the way the score is computed changes. A dash means the knob does
not apply. The published value sits between our mean and our maximum, and the
row that matches how it was computed exceeds it.}
\label{tab:estimator}
\begin{center}
\small
\begin{tabular}{lllc}
\toprule
Seeds & Aggregated & Selected over & CoLA MCC \\
\midrule
\multicolumn{4}{l}{\emph{Published \citep{mantri2026glot}}} \\
1 (seed 42) & --- & best $\tau$ of 3 & 47.49 \\
\midrule
\multicolumn{4}{l}{\emph{Ours, same method}} \\
15 & mean & --- & 45.37 \\
15 & max  & --- & 48.17 \\
1 (seed 42) & --- & best of 40 configs & \textbf{50.16} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{Matched-estimator comparison: best score at \textsc{Glot}'s own seed
($42$) over \textsc{Glot}'s own grid, $78$ configurations per task, so both
columns are computed the same way.}
\label{tab:repro}
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Task & ours & published & $\Delta$ \\
\midrule
CoLA (MCC)       & \textbf{50.52} & 47.49 & $+3.03$ \\
RTE (acc.)       & \textbf{59.57} & 59.21 & $+0.36$ \\
STS-B (Spearman) & 82.99 & 83.86 & $-0.87$ \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The published $\tau$-sensitivity curve cannot be read as a $\tau$ effect: it
spans $7.87$ MCC on CoLA, while we measure a spread of $5.46$ across seeds at
\emph{fixed} $\tau$.

\section{Main results}
\label{app:results}

\begin{table}[h]
\caption{Per-stage verdict. Entries are paired mean differences against
\textsc{Glot}'s flat baseline on shared seeds. The last column is \emph{not} a
fifth task: it is STS-B again on a decoder backbone, so it sits beside the BERT
STS-B column and the two read as a replication. $\dagger$ significant on both
the paired $t$-test and the exact sign test; $\ddagger$ additionally surviving
Bonferroni. \emph{No RTE entry is significant} --- that task has $277$
evaluation examples and a minimum detectable effect of $2.7$ accuracy points,
larger than any effect in this paper. ``---'' was not run.}
\label{tab:verdict}
\begin{center}
\small
\begin{tabular}{lccccccl}
\toprule
 & \multicolumn{4}{c}{BERT-base (encoder)} & TinyLlama & \\
\cmidrule(lr){2-5}\cmidrule(lr){6-6}
Arm & CoLA & STS-B & MRPC & RTE & STS-B & Verdict \\
    & (MCC) & (Spear.) & (F1) & (acc.) & (Spear.) & \\
\midrule
A   & $+0.70$ & $+0.03$ & $+0.04$ & $+0.02$ & $+0.04$ & flat everywhere \\
B   & $-1.26$ & $-1.07^{\ddagger}$ & $-0.33$ & $+0.99$ & --- & harmful \\
C   & $\mathbf{+1.42}^{\dagger}$ & $+0.20$ & $-0.32$ & $+0.55$ & --- & CoLA only \\
\midrule
AB  & $-1.24$ & $-0.75^{\ddagger}$ & $-0.28$ & $+0.79$ & $-3.56^{\ddagger}$ & harmful \\
AC  & $+0.45$ & $\mathbf{+0.24}$ & $-0.15$ & $+1.47$ & $+0.21$ & flat / slight $+$ \\
BC  & $+0.22$ & $-1.24^{\ddagger}$ & $-0.31$ & $+1.85$ & $-1.17^{\ddagger}$ & harmful \\
ABC & $-0.53$ & $-0.84^{\ddagger}$ & $-0.26$ & $+1.06$ & $-4.51^{\ddagger}$ & harmful \\
\midrule
\multicolumn{7}{l}{\emph{Every arm with B is negative on CoLA, STS-B and MRPC.
All four are positive on RTE, none significantly.}} \\
\multicolumn{7}{l}{\emph{Every arm with C but not B is positive on CoLA and
STS-B.}} \\
\multicolumn{7}{l}{\emph{The two STS-B columns agree in sign on all five arms
run on both backbones.}} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\begin{table}[h]
\caption{BERT-base, wide search, $n=15$ paired seeds. ``p/n'' counts seeds
positive/negative against the baseline.}
\label{tab:arms}
\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
 & \multicolumn{2}{c}{CoLA (MCC)} & \multicolumn{2}{c}{STS-B (Spearman)} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Arm & $\Delta$ & p/n & $\Delta$ & p/n \\
\midrule
C   & $\mathbf{+1.42}$ & 13/2 & $+0.20$ & 12/3 \\
AC  & $+0.45$ & 10/5 & $\mathbf{+0.24}$ & 11/4 \\
A   & $+0.70$ & 11/4 & $+0.03$ & \phantom{0}9/5 \\
\midrule
BC  & $+0.22$ & \phantom{0}7/8 & $-1.24$ & \phantom{0}0/15 \\
ABC & $-0.53$ & \phantom{0}6/9 & $-0.84$ & \phantom{0}1/14 \\
AB  & $-1.24$ & \phantom{0}4/11 & $-0.75$ & \phantom{0}1/14 \\
B   & $-1.26$ & \phantom{0}5/10 & $-1.07$ & \phantom{0}0/15 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Paired statistics on STS-B: B $\bar{d}=-1.065$, $t=-17.00$, sign
$p=6\times10^{-5}$; BC $-1.242$, $t=-8.30$, $p=6\times10^{-5}$; ABC $-0.837$,
$t=-6.36$, $p=0.00098$; AB $-0.747$, $t=-5.66$, $p=6\times10^{-5}$. All four
survive Bonferroni. On CoLA, C gives $\bar{d}=+1.416$, $t=4.11$, sign
$p=0.0074$ --- significant on both tests but above $\alpha=0.0063$.

\paragraph{The graph-free control.} We also ran an arm that deletes every edge
($\tau = 0.999$) while keeping the projection, the readout and the same $40$
tuning trials. It is statistically indistinguishable from the flat baseline on
all four tasks (CoLA $4/11$, $p=0.12$; STS-B $8/7$, $p=1.00$; MRPC $6/9$,
$p=0.61$; RTE $7/6$, $p=1.00$), and on CoLA it is numerically second of eight
arms at $46.20$, \emph{above} the baseline's $45.37$. At this budget the edges
contribute little to GLUE accuracy, so any claim about the graph's
\emph{geometry} has to clear this bar first. Stage C does not on CoLA ($+0.58$,
$8/7$, $p=1.00$), though it does on STS-B ($+0.16$, $12/3$, $p=0.035$); this is
the second reason we do not present C as established. Stage B is significantly
\emph{worse} than deleting the graph on CoLA ($-2.10$, $3/12$, $p=0.035$), which
is the sharpest available statement of its harm.

This does not contradict \textsc{Glot}'s own motivating result. The control here
still trains a pooler; it is not a bag of tokens. Against genuine bag-of-tokens
baselines the graph earns its place decisively (Appendix~\ref{app:stress}).
What the control shows is narrower: on these three GLUE tasks, at $40$ trials
per arm, the \emph{edges} are not where the accuracy comes from.

\section{Porting to other backbones}
\label{app:calib}

\begin{table}[h]
\caption{Edge density from the published grid $\tau \in \{0.1,0.3,0.6\}$, with
the tenth-percentile token cosine that produces it. Measured on 192 CoLA
sentences at the final layer, padding masked and self-loops excluded. This is a
property of the backbone and $\tau$ alone. Density near $1$ means the GNN sees a
nearly complete graph.}
\label{tab:density}
\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
Backbone & $\cos$ p10 & $\tau{=}0.1$ & $\tau{=}0.3$ & $\tau{=}0.6$ \\
\midrule
TinyLlama-1.1B   & 0.016 & 0.762 & 0.533 & \textbf{0.073} \\
ModernBERT-base  & 0.076 & 0.866 & 0.759 & 0.599 \\
BERT-base        & 0.098 & 0.876 & 0.505 & \textbf{0.103} \\
SmolLM2-360M     & 0.322 & 0.956 & 0.908 & 0.741 \\
RoBERTa-base     & \textbf{0.693} & 1.000 & 1.000 & \textbf{0.991} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The failure is layer-dependent: ModernBERT reaches $0.599$ at the final layer
but $0.996$ at layer 12. \citet{mantri2026glot} do not state which layer they
pool, so ``which backbones are affected'' cannot be settled from the published
description; we treat RoBERTa as the one clear case.

\begin{table}[h]
\caption{RoBERTa-base, $n=15$ paired seeds, tuned per arm and per task on
RoBERTa itself (40 trials each). \texttt{paper\_tau} pins the published grid and
gives the near-complete graph above; \texttt{density\_fix} is the quantile
repair. The repair is worst on both tasks, and on CoLA its deficit survives
Bonferroni --- despite having \emph{won} the tuning stage ($56.77$, best of
five).}
\label{tab:roberta}
\begin{center}
\small
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{3}{c}{CoLA (MCC)} & \multicolumn{3}{c}{STS-B (Spearman)} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Arm & mean & $\Delta$ & p/n & mean & $\Delta$ & p/n \\
\midrule
baseline              & \textbf{54.60} & \emph{ref} & --- & 83.89 & \emph{ref} & --- \\
\texttt{paper\_tau}   & 54.46 & $-0.14$ & \phantom{0}8/7 & \textbf{84.21} & $\mathbf{+0.32}$ & 12/3 \\
A                     & 54.37 & $-0.23$ & \phantom{0}5/10 & 83.80 & $-0.09$ & \phantom{0}5/10 \\
\texttt{density\_fix} & 53.19 & $\mathbf{-1.41}$ & \phantom{0}2/13 & 83.73 & $-0.15$ & \phantom{0}5/10 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

Forcing the graph sparse costs $1.41$ MCC ($t=-3.44$, $2/13$, surviving
Bonferroni at $\alpha=0.0125$). Against \texttt{paper\_tau} directly the repair
loses on both tasks ($-1.27$ CoLA, $-0.48$ STS-B, the latter $2/13$,
$p=0.0074$).

\paragraph{Density and scale are separate.} We had credited a $21.71 \rightarrow
27.15$ MCC recovery on ModernBERT/CoLA to density-matching plus median norm
rescaling, measured at one seed with both factors moved together. The full
$2\times3$ factorial over 5 seeds (Table~\ref{tab:fix}) separates them. The
choice of rescaling statistic matters far more than whether you rescale, and it
is the only effect here significant on both tests: on ModernBERT layer 12, whose
mean/median norm ratio is $7.66$, mean-based (\texttt{rms}) rescaling costs
$11.03$ MCC ($t=-5.47$, $0/5$) --- the mean is exactly the statistic outliers
corrupt. Median rescaling is directionally positive ($+5.96$ and $+5.32$ on
layer 12, $+1.34$ at the final layer with $5/0$) but reaches significance
nowhere at $n=5$. Density-matching has no consistent sign ($+4.58$, $-1.91$,
$-0.25$, $+0.45$). BERT is inert throughout, every contrast at most $0.27$ MCC
--- our earlier single-seed reading of that control ($45.54 \rightarrow 43.41$)
was noise.

\begin{table}[h]
\caption{Density $\times$ scale factorial, CoLA MCC, mean $\pm$ sd over 5 seeds.
Columns vary the threshold, rows vary token-norm rescaling. At $n=5$ the exact
sign test cannot fall below $p=0.0625$, so nothing here can meet our both-tests
bar; we report it as a lead.}
\label{tab:fix}
\begin{center}
\small
\begin{tabular}{llcc}
\toprule
Backbone (layer) & Rescaling & $\tau=0.6$ (pub.) & $q=0.05$ \\
\midrule
\multirow{3}{*}{BERT-base (final) \emph{control}}
  & none            & 45.25\,$\pm$0.97 & 45.00\,$\pm$1.51 \\
  & \texttt{rms}    & 45.01\,$\pm$1.25 & 45.03\,$\pm$1.18 \\
  & \texttt{median} & 44.98\,$\pm$1.00 & 45.08\,$\pm$0.83 \\
\midrule
\multirow{3}{*}{ModernBERT-base (L12)}
  & none            & 18.64\,$\pm$10.16 & 23.22\,$\pm$6.17 \\
  & \texttt{rms}    & \phantom{0}7.61\,$\pm$7.71 & 19.18\,$\pm$2.98 \\
  & \texttt{median} & 24.60\,$\pm$7.12 & \textbf{28.54}\,$\pm$2.41 \\
\midrule
\multirow{3}{*}{ModernBERT-base (final)}
  & none            & 37.53\,$\pm$1.09 & 35.62\,$\pm$6.00 \\
  & \texttt{rms}    & 38.64\,$\pm$1.48 & 37.61\,$\pm$1.72 \\
  & \texttt{median} & \textbf{38.87}\,$\pm$1.18 & 37.57\,$\pm$1.79 \\
\midrule
\multirow{3}{*}{RoBERTa-base (final)}
  & none            & 52.39\,$\pm$1.98 & 52.89\,$\pm$0.97 \\
  & \texttt{rms}    & 52.45\,$\pm$1.43 & \PEND \\
  & \texttt{median} & \textbf{52.54}\,$\pm$1.38 & \PEND \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\section{Decoder backbone}
\label{app:decoder}

TinyLlama-1.1B on STS-B, $n=15$ shared seeds, chosen over SmolLM2 on measured
geometry ($0.073$ density against BERT's $0.103$, whereas SmolLM2 sits at
$0.741$). Minimum detectable effect $0.503$.

\begin{center}
\small
\begin{tabular}{lccccc}
\toprule
Arm & mean & std & $\Delta$ & sign $p$ & pos/neg \\
\midrule
AC       & 80.15 & 0.54 & $+0.205$ & $0.302$ & 10/5 \\
A        & 79.99 & 0.51 & $+0.043$ & $0.607$ & \phantom{0}9/6 \\
baseline & 79.95 & 0.49 & \emph{ref} & --- & --- \\
\midrule
BC       & 78.78 & 0.95 & $-1.165$ & $0.00098$ & \phantom{0}1/14 \\
AB       & 76.39 & 1.87 & $-3.556$ & $6\times10^{-5}$ & \phantom{0}0/15 \\
ABC      & 75.44 & 3.58 & $-4.506$ & $6\times10^{-5}$ & \phantom{0}0/15 \\
\bottomrule
\end{tabular}
\end{center}

The ordering matches BERT, and the standard deviation grows in step with the
number of hyperbolic parts ($0.48 \rightarrow 0.95 \rightarrow 1.87 \rightarrow
3.58$).

\section{Noise diagnostic}
\label{app:stress}

We reproduce the signal-in-noise test of \citet{mantri2026glot}: a short signal
phrase carrying a logical dependency, injected into random distractor words,
with the distractor ratio swept from $20\%$ to $90\%$. BERT, mean $\pm$ std over
5 seeds; the original reports one seed.

\begin{center}
\small
\begin{tabular}{lcccc}
\toprule
Arm & 20\% & 50\% & 80\% & 90\% \\
\midrule
AC       & 95.0\,$\pm$3.4 & 88.2\,$\pm$11.2 & 90.8\,$\pm$5.8 & \textbf{88.5}\,$\pm$8.9 \\
C        & 95.0\,$\pm$3.3 & 87.6\,$\pm$11.2 & 90.8\,$\pm$5.9 & 88.4\,$\pm$8.8 \\
A        & \textbf{97.0}\,$\pm$1.3 & \textbf{91.9}\,$\pm$4.1 & \textbf{91.0}\,$\pm$2.7 & 84.9\,$\pm$9.1 \\
baseline & 96.0\,$\pm$0.5 & 90.2\,$\pm$5.0 & 88.1\,$\pm$4.7 & 79.8\,$\pm$10.8 \\
B        & 95.4\,$\pm$1.4 & 88.7\,$\pm$2.8 & 83.8\,$\pm$13.8 & 75.5\,$\pm$14.9 \\
\midrule
Mean           & 73.5 & 63.2 & 64.0 & 57.4 \\
\texttt{[CLS]} & 70.0 & 61.4 & 62.1 & 65.4 \\
Max            & 63.0 & 55.2 & 53.1 & 51.8 \\
\midrule
\multicolumn{5}{l}{\emph{\citet{mantri2026glot} Table 7, BERT row (single seed):}} \\
\textsc{Glot}  & 97.2 & 97.0 & 97.8 & 98.8 \\
\bottomrule
\end{tabular}
\end{center}

\textsc{Glot}'s advantage over bag-of-tokens pooling is real and holds in every
seed: mean, \texttt{[CLS]} and max lose $14$--$35$ points at every ratio, $0/5$
seeds positive. Stage B is again last among the hyperbolic arms.

\paragraph{We do not reproduce the published trained-pooler numbers.} At $90\%$
distractors the original reports $98.8$ where we measure $79.8 \pm 10.8$ for the
same method, and their curve is flat or rising under noise where ours falls. The
divergence is not uniform: the static poolers agree closely ($-2.2$ to $+5.6$
across twelve cells), while both \emph{trained} poolers diverge in opposite
directions, so the $37$-point margin their Table 7 reports for \textsc{Glot}
over AdaPool at $90\%$ becomes $-3.9$ in our hands. Their Appendix~B.4 does not
give the vocabulary source, the signal templates, or the pooler budget, so we
cannot localise the cause. We flag this as unresolved, and we never use the
published values as a baseline.

\section{GNN choice and training recipe}
\label{app:factorial}

\paragraph{GNN backbone.} Over 5 seeds at the published recipe, CoLA gives GAT
$45.38 \pm 1.19$, GCN $45.14 \pm 1.78$, GIN $44.86 \pm 1.33$; STS-B gives
$82.69 \pm 0.34$, $82.46 \pm 0.27$, $82.47 \pm 0.32$. Spread $0.52$ MCC and
$0.23$ Spearman, inside seed noise, and GAT is the only one of the three that
consumes edge attributes at all.

\paragraph{The STS-B residual is not a recipe mismatch.} The released README
trains differently from the paper's Appendix~B.2 (3 epochs,
\texttt{scorer\_hidden} $256$, $\tau = 0.8$ against $2$, $128$, $0.6$). We ran
the $2\times2\times2$ decomposition at seed $42$. Epoch count is the only factor
that moves anything ($82.36 \rightarrow 82.66$); $\tau$ and
\texttt{scorer\_hidden} are inert to within $0.04$. Over 5 seeds the two recipes
are indistinguishable ($82.69 \pm 0.34$ against $82.69 \pm 0.23$). The best
cell, $82.66$, is still $1.20$ below the published $83.86$, so the residual
stands.

\paragraph{Layer choice overfits.} Layer 8 beat layer 12 by $+4.62$ MCC on CoLA
but only $+0.25$ on STS-B, and lost on RTE ($-0.36$) and MRPC ($-0.46$).

\section{A retracted result of our own}
\label{app:narrow}

Under the graph-only search, Stage A on STS-B gave $\bar{d}=+0.223$,
$t(14)=6.20$, sign $p=6\times10^{-5}$, $15/15$ seeds positive, and replicated on
12 held-out seeds. It does not survive the wide search: $\bar{d}=+0.028$,
$t=0.69$, $9/5$. The effect was real but its cause was not geometry --- the
baseline had been pinned at $K=2$, $h=128$ while nothing else was, and it
recovers the whole gap once it can choose $K=4$. This is the clearest
illustration of the paper's own point: a pooling comparison is only as good as
the budget given to its control.

\section{Notes for reproducers}
\label{app:defects}

Three details of the released code cost us time. None affects the conclusions of
the original paper.

\begin{itemize}
\itemsep1pt
\item \texttt{jk\_mode=lstm} is accepted by the parser but crashes at runtime,
      and the advertised \texttt{mean} and \texttt{none} are not implemented.
      The usable space is $\{$\texttt{cat}, \texttt{max}$\}$.
\item Thresholding Poincar\'e \emph{distance} at a fixed radius gives an empty
      graph on real features (see \S\ref{sec:method}). The signature is a score
      that is bit-identical across a whole threshold grid. We use a quantile
      threshold everywhere for this reason.
\item The logged edge-density counts self-loops in the numerator but excludes
      the diagonal from the denominator, so an \emph{empty} graph logs
      $1/(n{-}1) \approx 0.10$ at CoLA's typical length rather than $0$. This is
      a reporting defect only --- no training path reads the statistic --- but it
      is a trap for exactly the audit this paper performs. It was corrected
      partway through the project, so the CoLA logs carry the offset and the
      later MRPC logs do not; densities are not comparable across campaigns
      without checking. Any density we quote is re-measured with the standalone
      instrument of Table~\ref{tab:density}, never taken from training
      telemetry.
\end{itemize}

\section{Structural graph variants}
\label{app:structural}

Since the geometric arms are flat on CoLA, MRPC and RTE, we also tried a
\emph{structural} prior: positional edges joining tokens with $|i-j| \leq w$,
positional-only graphs, their union with Stage A, and $k$-NN graphs. Tuning
maxima over 10 trials: baseline $45.32$, POS $47.08$, POS\_ONLY $46.80$, A\_POS
$44.87$, KNN $45.32$.

\textbf{These are tuning maxima and are not results.} At CoLA's noise level
($\sigma = 2.09$) the expected inflation of a best-of-10 is $\approx 3.22$ MCC,
which exceeds every gap in the list including POS's apparent $+1.76$ lead. We
report them because the arms are implemented and released, but draw no
conclusion. Confirming them on 15 paired seeds is the most obvious extension we
did not run.

\section{Limitations}
\label{app:limitations}

\begin{itemize}
\itemsep1pt
\item \textbf{Stage C's gain is not a clean geometry effect.} The search gave C
      a $3.6\times$ denser graph than the baseline ($q = 0.38$ against $0.10$),
      half the depth and half the width, so $+1.42$ MCC mixes curvature with
      those choices. It also does not separate from the graph-free control
      ($+0.58$, $8/7$, $p=1.00$). We are running the $2\times2$ that holds graph
      and architecture fixed and toggles only the message passing; until it
      lands, C is a direction, not a result. The gain does not reach MRPC and
      was never run on the decoder.
\item \textbf{We cannot say why the edges contribute so little on GLUE.} The
      graph-free arm matches the baseline on all three tasks, yet the graph is
      decisive on the noise diagnostic. Reconciling those two observations ---
      most likely by identifying which inputs actually need relational pooling
      --- would say more about \textsc{Glot} than anything in this paper. We did
      not attempt it.
\item \textbf{We select the epoch on the split we report}, because GLUE test
      labels are not public. This is a smaller version of the practice we
      criticise in \S\ref{sec:choices}. Splitting the development set into
      selection and reporting halves would remove the objection, and is the
      single most valuable improvement to this protocol.
\item \textbf{One reproduction residual is unexplained.} STS-B stays $0.87$
      short under every estimator we tried; we tested and ruled out the leading
      hypothesis (Appendix~\ref{app:factorial}).
\item \textbf{The scale factorial is underpowered.} At $n=5$ the exact sign test
      cannot fall below $p = 0.0625$, so no contrast in Table~\ref{tab:fix} can
      meet our own bar.
\item \textbf{Non-Euclidean layers may need structure-aware optimisation.} We
      gave every arm the full grid, and the search did move the learning rate
      per arm and per task rather than pinning it to a flat-tuned value. But we
      did not implement per-manifold Riemannian optimisation or curvature-aware
      initialisation. This is the strongest available objection to our Stage B
      negative, and we state it rather than merely acknowledging it.
\item \textbf{Every task we ran is small, and that may explain Stage B.} CoLA,
      STS-B, MRPC and RTE have $8{,}551$, $5{,}749$, $3{,}668$ and $2{,}490$
      training examples. Stage B hurts by amplifying seed variance, which more
      data suppresses, so its harm may not survive at scale --- and RTE, the
      smallest, is the one task where B-containing arms trend positive. We could
      not test this: at $495$ runs per task our design costs an estimated $170$
      hours for SST-2, $264$ for QNLI, and $38$ and $41$ days for QQP and MNLI.
      A reduced design --- four arms, confirmation seeds only, reusing the
      CoLA-selected configuration --- brings SST-2 to roughly $21$ GPU-hours and
      is the single most valuable experiment this paper is missing.
\item \textbf{RTE is underpowered and contributes nothing.} With $277$
      evaluation examples and a seed standard deviation of $3.36$, its minimum
      detectable effect is $2.7$ accuracy points. All seven hyperbolic arms are
      nominally positive there and not one is significant. We include it for
      completeness and base no conclusion on it.
\item \textbf{Coverage.} \citet{mantri2026glot} report ten GLUE tasks on six
      backbones, plus IMDB and seven MTEB tasks; we report four GLUE tasks on
      BERT, two on RoBERTa, one on TinyLlama, plus a synthetic noise task, and
      no MTEB or IMDB.
\end{itemize}

\end{document}
